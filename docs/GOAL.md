# Hive — Submission goal

Deliver a working, shareable multiplayer coding agent and an evidence-backed take-home presentation.

**Latest recheck — September 10 Pacific:** message actions remain in draft [PR #5](https://github.com/spinsirr/hive/pull/5), not the canonical production release. Head `b0ab5e8` has green [CI](https://github.com/spinsirr/hive/actions/runs/34564150694) and a successful [Vercel preview](https://vercel.com/spinsirrs-projects/hive/G9MMqXcTdGL3TDscEEoAwFLAxi9q). A fresh production page still reaches Live and preserves the prepared two-file result and actual Exit 0 / 125-test command. The later documentation-only finalization and its verification are recorded in [the finalization preflight](POST_REVIEW_VERIFICATION.md#september-10-pacific--scope-freeze-preflight). Fresh-account acceptance, second-account recheck, independent review and the narrated rehearsal remain distinct open gates.

**Confirmed presentation:** September 14, 2026, **9:30–11:00 AM PDT**. The four-item submission deadline is **September 13, 9:30 AM PDT**. See the [submission packet](SUBMISSION.md); repository visibility and external sending still require explicit owner approval.

### Final scope freeze — September 10 Pacific

After rereading the original recruiter email and official guide, Spencer confirmed that no more product features should be added. The guide's roughly six focused hours refers to the whole assignment, not a separate design phase. Hive already exceeds that intended scope; do not call the accumulated build a six-hour effort. Remaining work is core-flow verification, blocking fixes, focused code explanation and submission preparation. Memory, subagents, model controls and preview-only message editing are supporting/Q&A material, not additional live-demo segments. Preserve their honest verification limits; do not claim that moving them out of the demo makes an untested feature pass.

### September 10 owner-requested addition: bounded subagents

After the scope freeze below, Spencer explicitly requested research/review subagents and Codex Desktop-like API controls. The bounded implementation and evidence are recorded in [SUBAGENTS.md](SUBAGENTS.md): two research/review child tasks per turn, inline progress/results and child Stop, using the existing harness and shared conversation. His follow-up removed blanket inheritance restrictions: children reuse the current task's model, tools, skills and permission configuration instead of adding a separate access tier. Commit `e24bbab` is deployed. The September 10 live attempt retained shared state and errors across two browsers, but reached Gateway 429 without an observed child start; delegation/Stop/result acceptance remains open. Both browsers used the same account. No other frozen features are reopened by this addition.

## Current scope — 2026-09-05

The current user-provided goal explicitly excludes PR creation and multi-team management. Complete one real coding task, shared review, two-account collaboration, recovery, and the take-home materials. The September 4 discussion proposed PR handoff; that proposal is superseded for this goal and is not a completion gate. Normal authorized pushes and Vercel deployments of Hive itself remain part of implementation, not a product PR feature.

### Submission scope freeze — September 8

After reviewing Workflow's benefits and integration costs, Spencer explicitly chose not to expand this submission: **do not integrate Vercel Workflow, replace the execution architecture, or add product features.** Remaining engineering is limited to fixes and verification of the existing end-to-end flow, plus presentation and submission preparation.

Request-bound execution and unverified automatic recovery after hard worker termination remain disclosed limitations, not new implementation gates. This does not waive the existing browser-reconnection and caught-failure-continuation requirements, the approximately 20-minute rehearsal, reviewer access, or the four submission items. Repository publication and external sending still require separate owner approval. The [Workflow assessment](WORKFLOW_TRADEOFF.md) remains retrospective research and a possible post-submission direction, not a migration plan or a claim that Workflow was rejected at the beginning.

**Subsequent owner-authorized exception — September 8:** Spencer requested Sandbox/multiplayer-aware agent skills and tools, then Mem0 memory. Keep this to one bundled collaboration skill, task-context/Thread-reply tools and opt-in short, attributed repository memory. It does not reopen Workflow, PRs, issue triage, uploads, or multi-team administration. Local controlled verification is separate from deployment and actual Mem0 Cloud acceptance.

- [x] Implement and locally verify the four scoped tools and native skill without restarting existing Codex history. Actual Codex and MCP transport were used with controlled loopback model/Mem0 responses; real local Postgres verified attribution, access fencing, retry-safe replies and private-history exclusion.
- [x] Configure the server-only Mem0 key and redeploy production. The owner saved `MEM0_API_KEY` as a Production Secret; deployment `7qEBCLjXZ84pW8DLZbzSzvxHYuw2` is Ready. This confirms configuration/deployment, not Mem0 service acceptance.
- [x] Complete tool-signing configuration with owner approval. The owner saved `HIVE_INVITE_SECRET` as a Production Secret; deployment `B2qetSfWTF6rYrgc2dSFG53CKizw` is Ready. Live signed agent-tool callbacks returned 200/202 and an anonymous probe returned 401. Outstanding old invitation links are invalidated; existing membership is unchanged.
- [x] Verify one explicitly authorized short contribution saved to Mem0 Cloud and automatically recalled in another task on the same repository. The September 10, 01:12–01:14 UTC bounded round produced one ADD and a subsequent SEARCH with one result; the second task answered the marker, author and reply ID without receiving them in its prompt. Both turns completed and showed no commands. See the [live evidence](PRESENTATION_NOTES.md#september-9--live-memory-save-and-cross-task-recall).
- [x] Correct and verify the recalled source-task citation. After Fable's `30f692e` fixes, the September 10, 05:07 UTC retest in a brand-new task correctly returned the source task, message, reply, author and marker without any answer values in the prompt. Mem0 recorded one SEARCH and no new ADD; a fresh page retained the answer. This is one bounded positive provenance check, not a guarantee for every future answer. See [post-review verification](POST_REVIEW_VERIFICATION.md#live-reset-and-memory-source-citation).
- [x] Fix refreshed message timestamps locally. The owner-requested follow-up makes the actual React hydration reproduction pass, preserving local times, legacy labels and prop updates across four zone/season scenarios. The regression is now included in the passing full suite. See [local fix evidence](POST_REVIEW_VERIFICATION.md#follow-up-local-timestamp-fix).
- [x] Deploy the timestamp fix and verify a fresh production page. `e24bbab` deployed on September 10; fresh in-app and reloaded Chrome pages retained Pacific message times instead of server UTC labels. See [production evidence](POST_REVIEW_VERIFICATION.md#follow-up-production-timestamp-check).

## Completion checklist

**Owner-authorized subtraction — September 9:** removed manual Complete/Reopen and the dashboard's Active/Completed filters. Run results and diff approval remain; neither closes the conversation. Existing history and historical database fields are preserved without migration. Earlier completion/reopening checklist entries below are dated evidence of the former design, not current product requirements or rehearsal steps. See the [decision and regressions](PRESENTATION_NOTES.md#september-9--execution-completion-is-not-conversation-closure).

**Owner-authorized onboarding correction — September 9:** first GitHub signup may create an independent task without an invitation. Existing tasks still require membership/invitation; repository listing and attachment must use the current account's GitHub permissions. The agreed three-entry check passes against real local Postgres and actual routes with controlled GitHub responses. `be1922f` is deployed; the existing owner's real reconnect/create/attach flow and anonymous production denials passed. Fresh-account and authenticated cross-account production acceptance remain separate, open checks; no multi-team administration is added. See [onboarding evidence](PRESENTATION_NOTES.md#september-9--open-signup-with-task-scoped-collaboration).

**Current availability — September 9 Pacific:** the owner upgraded the correct Neon integration to Launch, and fresh production task access recovered. The bounded two-account check verified Live / 2 online, typing, refresh-preserved draft/history and an attributed human-only Thread reply. The prepared coding task's two-file diff and recorded Exit 0 / 125-test command were preserved. Mem0 and tool-signing configuration are deployed. Actual cloud save and positive cross-task recall passed; the later post-Fable retest also correctly distinguished the source task, message and reply. Lost-run recovery passed real local Postgres concurrency/fencing checks. The refreshed-message timestamp defect is fixed locally with passing actual React hydration regressions, but deployment and a fresh production check remain pending. This does not establish that rate limits are permanently resolved or that hard worker termination was tested live. The timed rehearsal and owner-controlled publication/submission gates remain open. See the [recovery evidence](PRESENTATION_NOTES.md#september-9--quota-recovery-and-two-account-recheck) and [post-review verification](POST_REVIEW_VERIFICATION.md).

The [quota incident](PRESENTATION_NOTES.md#september-8--neon-egress-incident) and [03:54 UTC release preflight](PRESENTATION_NOTES.md#september-9--sample-task-affordances-and-release-preflight) remain dated historical evidence. Release `96d1542` deployed the preview correction, connection-owned presence and task-scoped tools; deployment alone did not restore the exhausted database quota.

- [x] Complete a small change in a real attached repository; verify the checks, Runs, Files, and Diff (recorded real check: formal task, 2026-09-07 00:19 UTC; 92 sandbox-revision tests, TypeScript, and diff check passed in one command; both accounts previously inspected the workspace). This historical pass does not satisfy the current evidence gate below.
- [x] Make Invite and Complete accessible at narrow viewport widths (375px local interaction fixture; production controls verified at 737px, then actual Invite copy and cross-account Complete → refresh → Reopen verified at 375 × 812 on September 8).
- [x] Verify two real GitHub users sharing a task: attribution, annotations, steering during a run, queue ordering, and synchronization (original Joseph-author / owner-applier case passed at 07:09 UTC; the earlier queued message was applied once at 06:44).
- [x] Verify refresh, reconnect, and continuation after a failed run preserve messages and execution context. In addition to failed-run continuation and offline-page reload recovery, the 07:28 UTC no-reload check recovered one missed teammate message and the unsent draft, with a new live WebSocket handshake and no agent wake-up.
- [x] Add a read-only Monaco file viewer with file navigation and syntax highlighting (production changed-file snapshot rendered for the completed accessibility change).
- [x] Add and verify teammate autocomplete when typing `@` in the conversation composer (production task, actual teammate `josephmreb1`).
- [x] Finish loading, empty, and error states plus task completion and reopening (controlled delivery/empty/error checks, live retained 429 state, deployed latest-turn command empty state, and cross-account lifecycle check).
- [x] Verify the production task's Complete → refresh → Reopen cycle with owner approval (latest: 2026-09-06 07:13–07:14 UTC): owner completion reached both browsers; Joseph refreshed the read-only task and reopened it; the owner's controls recovered and the transcript/diff remained.
- [x] Prevent approval of empty or ineligible diffs in both the state machine and UI; verified with regression tests and the real local component. Production rollout is recorded separately in the evidence log.
- [x] Prepare the README, one-to-two-paragraph summary, decision log, and AI collaboration evidence; the [submission packet](SUBMISSION.md) links the material and explicitly lists unverified claims.
- [x] Release the Thread/whole-thread steering, paired rollback, dashboard and UI-demo changes with owner authorization. `fec2484` received a successful Vercel status on September 7; the canonical dashboard/demo, Thread rendering and retained workspace passed the bounded live UI check.
- [x] Ship the follow-up file icons/cache, inline-thread layout and conversation-scroll fixes. UI release `396d80f` received a successful Vercel status; 125 unit tests, controlled runner/route/component regressions, lint, typecheck and the production Webpack build passed.
- [x] Obtain fresh, passing command evidence before rehearsal. On September 8 at 15:01–15:02 UTC, the independent task executed one exact `pnpm test && pnpm typecheck && git diff --check` with Exit 0. Both real accounts inspected all 125 sandbox-revision unit tests, controlled regressions including default-name/caller-override scrolling, and TypeScript output. The two-file diff is retained; this is fresh evidence, not the formal task's historical 92-test result.
- [x] Deploy the owner-approved Luna coding-model switch without changing planning, authentication, reasoning effort or task history. `9662380` deployed successfully; the pinned native process verified model-switch resume, real command execution and bounded retries against a local fixture. The one production request confirmed Luna selection but was denied by the free Gateway tier. This is configuration/compatibility evidence, not successful live model execution.
- [x] Replace that coding default with the owner's selected Free Tier-eligible GPT-5.1-Codex Mini. `9953244` deployed successfully and actual Gateway requests returned 200 for the exact model. No credit purchase, automatic model fallback, authentication change or task reset occurred. The later 15:01–15:02 UTC continuation passed the coding acceptance; this is not unlimited model access.
- [x] Remove duplicate prompt context while retaining native resume and human authorship. `a13b3a1` deployed successfully after three new red-to-green regressions, all 135 local unit tests, controlled checks, typecheck, lint and the production build passed. Current requests are included once; resumed coding does not replay public agent replies already owned by native history. No history reset, forced compaction or retry-policy change was introduced.
- [x] Fix and verify bounded HTTP-level 429 recovery without replaying a native turn or completed command. The pinned real Codex regression passes; `f3f602f` deployed successfully. The September 8 live attempt made two delayed retries totaling 51.219 seconds before stopping on persistent 429. This verifies bounded recovery behavior, not removal of provider/account limits or a passing coding task.
- [x] Correct misleading command verdicts: `4cf91a2` deployed **Exit 0 / Exit N** labels rather than **Passed**. A fresh production page showed the same five recorded commands and retained expandable syntax-error output. The real-component regression passed after first reproducing the misleading label; this UI correction is not a coding-task pass.
- [x] Verify new two-account Thread discussion, frozen whole-thread queuing during a run and safe-boundary application. On September 7, Joseph queued two replies; a later third reply stayed outside that steer, and Apply remained disabled until the baseline run ended.
- [x] Execute a real paired-checkpoint round trip in the independent task. Baseline and partial-work restores changed the actual diff, dependency directory and recorded commands, preserved all three discussion replies, and did not automatically rerun or approve failed work.
- [x] Restore a paired checkpoint with a nonempty pending queue in production. On September 8 at 14:41 UTC, restoring the current failed-run checkpoint preserved Joseph's one pending read-only steer, author, all discussion, both changed files and four recorded commands in both accounts. Nothing reran and completion remained disabled. This tests queue preservation, not successful native continuation.
- [x] Finish the whole-thread coding task with its requested regressions and real checks; verify successful attributed continuation in the same restored Codex task. The 15:02 UTC final response correctly separated Joseph's behavior requirement from Spinsirr's checks and left the third localization discussion unimplemented. The supervising AI first reproduced and supplied the test repair; this was not an autonomous first-pass success or a native-history-only isolation test. Both authenticated accounts then verified shared approval and Complete → refresh → Reopen without losing the successful command or diff.
- [x] Have the owner confirm the summary and reconcile verification results. Spencer confirmed the existing two-paragraph project introduction on September 8, 2026; its wording remains unchanged and the current technical acceptance results are recorded. This is not authorization to publish the repository or send the packet.
- [x] Resolve the failed incremental-text acceptance check: native app-server deltas deployed in `4a1abb3`. Production observation at 2026-09-07 00:19 UTC recorded 25 growing updates before completion, one final reply and successful real checks. A fresh page restored the identical reply. See [diagnosis and acceptance](STREAMING_DIAGNOSIS.md).
- [x] Check read-only presentation navigation and retained artifacts on production. The original 170-second preflight is recorded; the September 8 route reconciliation rechecked two actual identities, the frozen two-of-three-reply boundary, current partial Diff/Runs, full Files navigation and disabled unmatched checkpoints. The [20-minute script](PRESENTATION_NOTES.md#demo-sequence-20-minutes) now targets the independent task. This is not the full narrated rehearsal below.
- [x] Prepare a compact [presenter card](DEMO_BRIEF.md), verify the current successful task/Thread and public demo paths, and perform credential-pattern screening across all 80 locally reachable commits at `ce2741b`. No real secret was confirmed; the remaining matches were example URLs and a labeled test fixture. This does not substitute for owner review, a comprehensive security audit or publication authorization.
- [ ] Rehearse the approximately 20-minute demo and verify the deployed app and submission links.
- [ ] With the user's approval, make the repository public and send all four submission items at least 24 hours before the presentation.

Record completed checks and their limitations in `PRESENTATION_NOTES.md`. Distinguish implementation from live verification. Two-user testing requires two real authenticated accounts.

## Current rehearsal evidence

**September 9 update:** production availability and bounded two-account recovery were rechecked after the owner's upgrade; see the [current evidence](PRESENTATION_NOTES.md#september-9--quota-recovery-and-two-account-recheck). The task now offers **Approve changes** after later activity, rather than remaining in September 8's approved state. No new approval, model run or narrated rehearsal was performed by this recheck.

**September 8 final preflight:** both real identities, the frozen two-of-three-reply Thread, successful Runs and two-file Diff remained available. A 375px production lifecycle/invite check passed, as did unchanged-file navigation and colored read-only Monaco. Local `pnpm test` passed all 135 unit tests and controlled regressions; typecheck and lint passed. The [dated preflight](PRESENTATION_NOTES.md#september-8--scope-freeze-and-final-technical-preflight) is not a timed narrated rehearsal. No new model run occurred, and the prepared coding result was preserved.

**Latest result — September 8, 15:01–15:10 UTC:** the independent task is now a passing prepared result. After the isolated regression repair and deployed prompt deduplication, its one combined check returned Exit 0 with 125 sandbox-revision unit tests, real-component regressions and TypeScript passing. Both accounts retained the same attributed final response, two-file diff and actual output. Joseph's authenticated browser approved it, the owner's browser completed it, and Joseph refreshed and reopened it; this acceptance was operated by the supervising assistant, not two independently acting human reviewers. The task is left open with its workspace approved and the successful Runs command intact. No new text-only turn, restore, model request or sandbox push followed. Gateway recorded two successful calls, not proof that rate limits are fixed. The chronology below preserves earlier failures rather than presenting them as the current state.

Read-only preflight on **2026-09-07 23:23 UTC** confirmed successful deployment of `396d80f`, HTTP 200 for the homepage and `/demo`, HTTP 401 for the unauthenticated formal-task API, and a private GitHub repository. Both inspected browsers were signed in as **Spinsirr**, not two different users. The formal task now shows **Approved** and **No commands in this turn** after a later text-only steer. Do not reset or restore the approved task to manufacture demonstration evidence.

That preflight did not perform a restore, and the initial independent-task request was denied. The owner subsequently explicitly authorized a separate acceptance task and real Thread/rollback testing, without changing the approved task or pushing test code. On September 7 at 23:43 UTC, **Label the return-to-latest message action** was created; **Spinsirr** and **josephmreb1** joined through real authenticated browsers. The repository was attached after creation. The earlier authorization and second-account blockers are resolved.

The two-reply Thread queued during a read-only baseline, remained frozen when a third reply was added, and was applied after the baseline ended. Implementation hit Gateway 429 twice; one bounded continuation produced a one-line default accessible label and installed locked dependencies, but did not add the requested regressions or run tests/typecheck. Model requests were stopped after the second failure. The first real restore returned to the clean baseline around September 8, 00:06 UTC; the second returned to partial work by 00:16 UTC. Diff, actual dependency-directory presence and Runs tracked the selected checkpoints, while discussion and steering attribution remained. At that observation the task remained partial and unapproved; the later queue/continuation checks are recorded separately above and below.

After deploying `f3f602f`, one authorized post-restore continuation ran at **02:36–02:38 UTC on September 8**. Five successful Gateway calls were followed by persistent 429s despite 17.455s and 33.764s of backoff. That turn retained five commands, including two syntax-error edits hidden behind zero exit codes by `|| true`. No new regression or test/typecheck command was produced; the one-line diff and discussion were retained. The native turn resumed, but this is not proof of a successfully completed context-aware task.

The owner subsequently approved **`openai/gpt-5.6-luna`**, preserving authentication and the existing task. After `9662380` deployed, the original acceptance task made one request at **03:48:01 UTC** (`gen_01M1ZJ2GHYHS7PHK31BY7EYT7R`). Gateway returned **403**, explicitly stating that free-tier users cannot access this model and that paid Gateway credits are required. No model usage or inference charge was reported. Both real accounts retained the attributed request and all three Thread replies; the source diff remained unchanged and current Runs showed **No commands in this turn**. No further request, automatic model fallback, billing change, credential change, approval or completion occurred. The next live coding check requires the owner's direction on Gateway credit entitlement or an eligible model; a site's Vercel Pro subscription is not the same as the Gateway paid-credit tier.

After the owner reported completing billing, the team's dashboard showed **Pro**. One continuation at **04:07:00 UTC on September 8** still returned **403** for Luna (`gen_01M1ZK5919ZK4ZJA5F3TYQR8P3`), with no reported model usage and zero inference cost. At **04:13–04:15 UTC**, the Gateway overview still labeled its balance **Free Credit**. Opening that balance exposed **Buy AI Gateway Credit**, a separate one-time top-up dialog. The assistant did not choose an amount, continue to payment, change auto-reload or send another model request. The next action is owner confirmation/completion of paid Gateway credits, not another Pro upgrade or a repeated coding attempt. No acceptance gate is closed by this billing observation.

The owner then declined further credit purchase and explicitly selected **`openai/gpt-5.1-codex-mini`**. The official model catalog's active Free Tier filter showed this exact model as eligible. The coding default and example configuration now select it; planning, low reasoning, authentication, retries and task identity remain unchanged. All 132 unit tests and controlled regressions, typecheck, lint and production build passed locally. The pinned real Codex loopback check also verified first-turn history, a real command and bounded failure recovery after switching to this model; the original Luna fixture still passes. Commit **`9953244`** deployed successfully to production, independently of these local passes.

One continuation of the existing task at **04:26–04:28 UTC on September 8** reached GPT-5.1-Codex Mini successfully: three Gateway requests returned **200**, including `gen_01M1ZM9AQT2K35Y5ZNSTVSH78X`. Runs retained two inspections and one exact `pnpm test && pnpm typecheck && git diff --check` command with **Exit 1**. The sandbox revision's **125 unit tests passed**, but `scripts/check-conversation-scroll.mjs:92` failed to parse because `defaultBtn` was declared twice. The Diff confirms duplicated regression blocks alongside the retained source-label addition. Because the command uses `&&`, typecheck and diff-check were not reached. A subsequent Gateway request hit **429**; two bounded retries waited **16,249ms** and **30,023ms**, then stopped. The observed diagnostics do not identify the exact quota window or imposing provider. Both real accounts retained the same failure, request and three authored replies; Files remained available. No final model explanation, successful acceptance, approval or completion is claimed. Purchasing credits is no longer the chosen next action, and this observation did not trigger another model request.

The September 8 **14:32–14:34 UTC** continuation removed the duplicate regression block but exposed the next real failure at `scripts/check-conversation-scroll.mjs:84:32`: the test queries a button before scrolling away from the bottom, where the actual component intentionally hides it. Its separate override mount also lacks the required Conversation provider. Runs retains three inspections and the exact combined check with **Exit 1**; no typecheck or diff-check pass is claimed. Five Gateway requests succeeded between **14:32:26 and 14:33:09 UTC**, each reporting **41.9K–45.3K input tokens**, followed by 429s at **14:33:30, 14:33:45 and 14:34:17**. The last request is `gen_01M20Q1VNWB4E32PNTBY5B1QKT`; routing details and the specific limiting quota were unavailable. The overview still showed **24.69 Free Credit**. This supports rate limiting despite remaining balance, not an exact RPM/TPM or provider diagnosis, and no further model request was made.

During that run, **josephmreb1** saved and explicitly queued one read-only review in a new Thread. The owner saw its author and text with Apply disabled while running and enabled only after failure. At **14:41 UTC**, the owner confirmed restoring the current **10:34 AM EDT** paired checkpoint `snap_eEToiKnth8keB0PHY3Sf6aOcWMBh`. Both real accounts retained the same pending steer, original three-reply discussion, two-file diff and four commands; the restoration message appeared without a model run or enabled completion. The earlier empty-queue filesystem round trip and this nonempty-queue preservation are separate observed checks. The user confirmed that the presentation is **not yet scheduled** and asked to prioritize finishing quickly; no calendar submission deadline is invented.

After that preservation check, the supervising assistant removed only the diagnostic pending steer, leaving Joseph's reply intact. An isolated copy reproduced the hidden-button/provider test defects; the corrected real-component regression was green, red when the source label was removed, then green when restored. Hive received that concrete repair recipe and made the two-file change at 14:52–14:54 UTC, but another 429 arrived before checks. After `a13b3a1` deployed, the final check-only turn at 15:01–15:02 UTC passed without more edits. Gateway showed two 200 responses with 53.7K and 56.1K total input-context tokens; the latter included 53,632 cached tokens and 2,426 noncached tokens. Existing native history still grows, so no measured total-context reduction or resolved-limit claim is made.

The September 7 checks at **17:43 and 18:04 UTC** did show the original accessibility Diff, full Files tree and successful 92-test sandbox command. Those observations remain valid historical evidence, but the current latest-turn Runs view has since changed. The genuine combined check ran at **00:19 UTC**, after the native-streaming fix. Recheck actual artifacts before rehearsal; do not substitute fixture artifacts or a chat summary for command output.

The original two-account attribution failure is fixed and retested. On September 6, Joseph saved the same annotation wording at 07:04 without waking Hive, queued it during the owner's real check at 07:05, and waited while Apply was disabled. After the owner applied it at 07:08, both browsers received the correct Joseph-authored answer at 07:09. Together with the retained ordinary message's single execution at 06:44, this closes the original multiplayer sequence gate, not the newer whole-thread acceptance gate.

The multiplayer and recovery checks used `356c525`, including the attribution fix and corrected command empty state. After crossing the connection's normal rollover offline, Joseph recovered one missed human-only message exactly once, retained the draft, and established a new 101 live WebSocket connection without refreshing. Offline-page reload during another teammate's run also recovered the task. Test network settings were restored, drafts cleared without sending, and DevTools closed. The subsequent incremental-text check initially failed; `4a1abb3` now passes it on production. This last retest used the owner's page and a fresh owner page, not a new two-account or offline-mid-delta rehearsal. The timed rehearsal remains open; repository publication and submission still need owner approval.

## Remaining boundaries

One team, one task per session, at most one repository attached at any time after creation. Agent conversation remains primary; thread replies and code annotations require explicit promotion to steer, individually or as a frozen whole discussion. User-requested checkpoint rollback restores files and agent context together without rewinding team conversation or running the queue. Production checks cover discussion/queue boundaries, paired restore including a nonempty queue, successful assisted coding continuation, and shared review. The timed narrated rehearsal and owner-controlled publication/submission gates remain open. The subsequently requested repository-scoped Mem0/tools addition passed a real cloud save, positive cross-task recall and the post-review source citation retest. Live cross-repository isolation remains untested. The timestamp fix is deployed and the fresh-page check passed; live subagent delegation/Stop/result acceptance remains open after the September 10 Gateway 429. Image/file uploads, multi-team administration and PR creation remain excluded. Keep the repository private until the user approves publication; do not send submission messages without authorization.
