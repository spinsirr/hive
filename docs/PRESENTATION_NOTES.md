# Hive — Presentation Notes

This is the source of truth for the take-home story. Update it when a product decision changes or a claim gains evidence.

## Submission checklist

Send all four at least 24 hours before the presentation:

- Public GitHub repository with readable commit history.
- Live deployed URL.
- README covering the problem, decisions, and AI collaboration.
- One-to-two paragraph summary blurb.

The presentation is 90 minutes; the product demo is about 20 minutes in this order: problem, solution, code, AI journey.

Current app: [hive-roan-mu.vercel.app](https://hive-roan-mu.vercel.app/)

## Release boundary

On September 7, the owner authorized pushing and deploying the simpler homepage, retained `/demo`, Reply/Thread UI, whole-thread steering and paired rollback in `fec2484`. Follow-up UI fixes shipped in `396d80f`; bounded Gateway recovery shipped in `f3f602f`; accurate Runs exit-code labels shipped in `4cf91a2`. Prompt deduplication shipped in `a13b3a1`. On September 8 at 15:01–15:02 UTC, the independent task completed its exact combined check with Exit 0 and a correctly attributed final response. Two authenticated accounts inspected the actual two-file result and successful output, then verified shared approval and Complete → refresh → Reopen. Earlier checks cover frozen Thread/queue boundaries and paired restores, including a nonempty queue. This closes successful assisted coding continuation, not recurring 429 risk or the timed narrated rehearsal. The UI demo is a dashboard preview; the unrelated approved formal task remains untouched.

The owner-approved Luna switch deployed in `9662380`, but the account's free-credit tier rejected it with 403. The owner then selected **GPT-5.1-Codex Mini**, deployed in `9953244`. The latest successful check used this same model and retained task history. Current Runs contains the passing check from the sandbox's older revision: **125 unit tests**, controlled regressions and TypeScript. Do not confuse that count with the deployed application's **135 local unit tests**, or claim that later success eliminates rate limits.

## Summary blurb

The current two-paragraph introduction lives in the [submission packet](SUBMISSION.md#summary-blurb), alongside all four deliverables and the remaining access gates. Spencer confirmed this existing wording on September 8, 2026. The text was not rewritten as part of that confirmation; repository publication and external sending remain separately unauthorized.

## Problem

Start with the workflow that motivated this build: one person owns the prompt, transcript, and workspace; teammates enter after the agent has already acted. The team copies context across private agent sessions, chat tools, screenshots, and pull requests, losing both intent and clear authorship.

The missing primitive is not another team chat. It is a shared agent session where several humans can see the same work, discuss it in context, and control one agent without racing to overwrite one another.

## Solution

Hive makes the agent conversation the primary product surface:

- Every normal message addresses Hive; an explicit `@teammate` mention remains human discussion.
- Every human message has server-authoritative authorship.
- A teammate can open **Reply** or **Open thread**, discuss a message and retain each reply's author without waking Hive.
- **Steer Hive** promotes one reply; **Steer thread** promotes the discussion through its current last reply. Later replies are not silently added to an already queued steer.
- Steers created during a run enter an attributed, ordered queue and wait for a safe boundary.
- Everyone sees the same transcript, presence, repository, run state, files, command output, and diff.

## Domain model

- **Team:** people collaborating through explicit task invitations.
- **Task Session:** one intended outcome, one shared transcript, and at most one repository.
- **Repository Access:** repositories authorized for the current GitHub account and App.
- **Attached Repository:** selected at any time after a task begins and immutable within that session.
- **Thread:** authored replies attached to a human or completed agent message.
- **Code Annotation:** a quoted file-and-line selection shared for discussion, not a file edit.
- **Steer:** one reply or a frozen discussion explicitly promoted into agent direction.
- **Run:** one execution turn against the attached repository.
- **Workspace:** evidence of what the agent actually did.

This correction matters: a session is not a permanent channel or project. Its lifecycle is exactly one task; sharing it does not share the inviter's other tasks or repository access.

## Taste and scope decisions

| Decision | Why it matters | Rejected alternative |
| --- | --- | --- |
| Make the agent conversation primary | Collaboration happens before and during execution, not beside it. | A team chat with a tagged bot. |
| One task per session | Gives the transcript, repository, and run state a clear lifecycle. | A long-lived room that accumulates unrelated work. |
| Allow conversation before repository attachment | Intent can be clarified before code access is needed. | Requiring a repo during session provisioning. |
| Separate Thread discussion from steer | Humans can discuss without accidentally redirecting the agent; a whole-thread steer has an explicit reply boundary. | Treating every comment as a prompt or silently extending an already queued instruction. |
| Queue concurrent steering | Multiplayer input stays visible, attributed, and deterministic. | Last-write-wins prompts or silent instruction merging. |
| Preserve distinct authorship through execution | A parent message author, annotation author, steer promoter, and person applying it can be different people. | Treating the person pressing Apply or the parent message author as the author of every instruction. |
| One repository per task | Keeps credentials, filesystem scope, and review evidence legible. | Ambient access to every team repository. |
| GitHub App for team access, OAuth for identity | Repository authority and human identity have different lifecycles. | Personal access tokens or browser-supplied identity. |
| Reuse AI SDK Harness + Codex + Sandbox | The differentiator is multiplayer control, not rebuilding an agent runtime. | A custom harness and container platform. |
| Let native history own prior agent turns | A resumed run retains its history; the product adds attributed human intent without replaying the public agent transcript or duplicating the current request. | Growing a second agent history inside every prompt or resetting context to lower usage. |
| Show artifacts, not a generic overview | Diff, workspace files, and the latest turn's commands each have a clear object and can be verified. | A workspace tab that mixes repository metadata, status, and summary counts. |
| Name the human need, not the implementation | Runs is an audit trail for teammates: command, outcome, duration, and expandable output. | Calling a read-only log “Terminal” and implying that humans should operate it. |
| Neon is canonical history | Native Codex history can disappear with compute; team intent cannot. | Treating sandbox files as the product database. |
| Quiet error state | A failure should not become a theatrical agent apology. | Large error bubbles that repeat internal details. |
| Neutral interface, semantic code colors | Syntax colors, file-type icons and green/red changes make the work readable; monochrome is for the surrounding interface. | Applying the black-and-white brand palette to every code token and diff line. |
| Replies readable beside the message | Show the author's reply under its parent; keep the Thread panel for continued discussion and whole-thread steering. | Hiding a one-line comment behind a reply count and a panel switch. |
| Quiet entry, live follow | Open saved conversation history directly at the latest message; follow new text smoothly and stop following when someone reads older messages. | Animating through the entire transcript every time a pane opens. |
| A small frontend task in Hive itself | An author-specific accessible label makes teammate intent visible in both the conversation and the diff. | A separate demo application or deployment workflow that needs its own setup. |
| Remove Vercel Connect experiment | The available install flow is developer-dashboard oriented; direct GitHub App onboarding fits users today. | Shipping a connector path users cannot complete. |

## Workflow tradeoff

**Reviewed September 8, 2026, after the build.** This is a newly researched tradeoff, not evidence that we evaluated and rejected Workflow at the start. The [sourced comparison](WORKFLOW_TRADEOFF.md) separates official capabilities, current code, and recommendations. No architecture change has been implemented as part of this review.

**Final submission decision:** Spencer subsequently chose to defer this integration and freeze the existing architecture. Follow the [submission scope freeze](GOAL.md#submission-scope-freeze--september-8); the recommendation below is a post-submission direction, not remaining implementation work for this goal.

The useful distinction is **collaboration state versus durable execution**, not Postgres versus Workflow as interchangeable products.

| Responsibility | What Hive currently does | What Workflow would change |
| --- | --- | --- |
| Authorship, membership, frozen Thread input, queue order and Apply permission | Postgres plus the task state machine owns these product rules. | Hooks can receive events, but we still define authorization, ordering and discussion-to-instruction policy. |
| Continuing work after the executing process dies | The session POST awaits the agent inside a request configured for 300 seconds. Caught failures retain recovery data where possible. | Durable steps can persist progress and schedule continuation; this is a capability the database row alone does not provide. |
| Native agent context and files | Hive pairs Codex resume state with Sandbox recovery points. | Official Harness workflow helpers support step/time-slice continuation, but cross-turn resume storage and consistent workspace recovery still need an explicit owner. |

Sources: [current session route](../src/app/api/sessions/[sessionId]/route.ts), [locked state transitions](../src/lib/task-session-store.ts), [runner/checkpoints](../src/lib/hive-runner.ts), [Workflow hooks](https://useworkflow.dev/docs/foundations/hooks), [Harness workflow utilities](https://ai-sdk.dev/docs/ai-sdk-harnesses/workflow-utilities).

**The cost of our current approach:** the state transition commits before execution starts. A process killed in that gap or during execution may never reach our error/checkpoint handler, leaving the task marked running. The inspected run path has no durable dispatch or automatic stale-run recovery. This is a static-code risk, not a production crash we induced or reproduced. Successful browser reconnection and caught-error continuation do not prove hard-process-crash recovery.

**The alternative deserves serious consideration:** `@ai-sdk/workflow-harness` already supports `HarnessAgent`, so adopting Workflow need not replace Codex. Its semantic-step and time-slice runners reduce custom orchestration work. Compatibility with Hive's native streaming bridge and paired restore path has not been tested. Durable retries also do not make arbitrary coding side effects exactly-once; commands may have taken effect before their result was saved. Stable execution IDs, safe continuation, duplicate-effect handling and database-to-run reconciliation remain necessary. [Harness utilities](https://ai-sdk.dev/docs/ai-sdk-harnesses/workflow-utilities), [idempotency](https://useworkflow.dev/docs/foundations/idempotency).

**Recommendation, not a shipped claim:** retain Postgres as the authority for team intent and permissions; evaluate Workflow for execution durability if reliable continuation across worker termination or long-running waits is a product requirement. Do not add a second independently authoritative steering queue. Validate commit-before-dispatch failure, duplicate delivery, interrupted coding steps and paired restoration before calling the integration reliable. Workflow would not remove model-provider rate limits.

### Short presentation explanation

> Hive 把多人协作状态放在 Postgres 里，明确谁说了什么、哪些讨论进入队列，以及什么时候可以执行。但保存了数据，不等于执行进程被终止后能够自动续跑。构建后的调研发现，Workflow 可以承接持久执行，并保留 Codex；接入仍要验证重试是否重复改代码，以及文件和 agent 上下文的一致性。我决定这次冻结 scope，不迁移执行架构，把剩余精力用于现有协作闭环的修复和验收。这是明确保留的限制和后续方向，不是已经具备的恢复能力。

## Demo sequence (20 minutes)

Use the compact [presenter card](DEMO_BRIEF.md) during delivery; this section retains the exact interaction prompts and failure boundaries.

Use the owner-authorized [Label the return-to-latest message action](https://hive-roan-mu.vercel.app/sessions/label-the-return-to-latest-messa-qvezdg) task, attached to `spinsirr/hive`. The intended result is small and inspectable: `ConversationScrollButton` has the default accessible name **Scroll to latest message**, a caller can override it, and the existing real-component regression covers both cases. Joseph owns the behavior requirement; Spinsirr owns the checks and no-push boundary.

**This is the target presentation route, not a completed rehearsal.** The current task has a verified source-label change, real-component coverage of both names and a passing combined check. It is open with an approved workspace and retained Runs evidence after the lifecycle check. Its implementation took failed attempts and concrete repair guidance from the supervising AI; describe that honestly. The earlier [formal accessibility task](https://hive-roan-mu.vercel.app/sessions/clarify-annotation-to-steer-inte-o5uwf0) is separate historical evidence. Do not reset, restore or send new work there to manufacture a current pass.

### Rehearsal gate — not yet complete

- The coding task, requested regressions, exact checks and attributed final response passed at 15:01–15:02 UTC. Nonempty-queue restore passed separately at 14:41 UTC. Preserve both dated results; the successful continuation used native resume plus the product's attributed human context, not a native-history-only isolation test.
- Use `Spinsirr` and `josephmreb1` in separate authenticated browsers on the same task URL. Check the identity menus, not only the online avatars. Both identities were rechecked on September 8; this does not verify a new reviewer's invitation/sign-in.
- Inspect current **Diff**, **Files** and the expanded combined command in **Runs** before starting anything. Read the actual output and exit code. **Exit 0** is a command result, not an automatic test verdict. The old 92-test result belongs to a different repository revision and is not a substitute.
- The new read-only Thread below is for rehearsing collaboration after implementation passes. Do not re-steer the existing three-reply implementation thread: its third, future-localization reply was deliberately excluded from the earlier two-reply steer.
- The owner-selected GPT-5.1-Codex Mini is deployed. The final check needed two successful Gateway calls; earlier longer turns exhausted bounded retries on 429. Plan for that risk rather than silently changing models, buying credits or replaying a failed task. Confirm the presentation date/time before calculating the 24-hour submission deadline. Keep the repository private and do not send invitations or submission material to outside reviewers without the owner's approval.

Read-only UI navigation is verified separately in the dated evidence log. Neither the earlier 170-second preflight nor the September 8 route reconciliation is a 20-minute narrated rehearsal. Record the actual start/end time, observed outcomes and any skipped steps when that rehearsal happens.

### 0–3 min — Problem

Start with two complementary requirements: Joseph defines the default accessible name and preserved caller override; Spinsirr requires real regression evidence for both and sets the no-push boundary. In private agent sessions, those intentions often meet only after code is produced. Hive puts both people in the same agent conversation while the work happens. The unit is one task, not a permanent chat room or a collection of agents.

### 3–10 min — Product

1. **3:00–4:00 — Shared task.** Open the signed-in dashboard, then the same acceptance task in both browsers. Point out the human identities and online count; Hive is not an online person. Show the saved repository-attachment message and explain that it happened after task creation. On narrow screens use **Conversation / Workspace**; **Close thread** returns from Thread to the previous surface.
2. **4:00–5:00 — Real result.** Show the prepared task's actual Diff, read-only Monaco file and expanded combined check. State clearly that the implementation was completed beforehand. Read the test count from Runs, not from memory or chat. Files browses the repository, not only changed files. Do not edit or add annotations just to navigate.
3. **5:00–6:00 — Discussion.** Type `@`, select Joseph and send `@josephmreb1 let's review the completed accessible-name change together.` Open **Reply** on that new message. Joseph and Spinsirr each send the read-only reply below. Both stay human-only; prepare Joseph's browser on this Thread before starting the check.
4. **6:00–8:00 — Shared control.** Spinsirr sends the one combined check below. While it is visibly running, Joseph clicks **Queue thread** once, not the individual reply's **Queue steer**. Both browsers should show one queued item with two replies and Joseph as the promoter; Apply remains disabled. Add the optional third reply only after the queue snapshot exists, and show that it remains a discussion outside those two replies. If execution finishes too quickly, call it an immediate-steer case; do not rerun just to manufacture a queue window.
5. **8:00–9:00 — Inspect, then apply.** When the check ends, inspect the actual command/output before Spinsirr clicks **Apply**. Verify that the resulting answer separates Joseph's behavior requirement from Spinsirr's validation requirement and does not execute the third reply. The reply-only turn has no commands; **No commands in this turn** is expected afterward. Runs does not display the earlier check on that turn; do not describe it as a history browser.
6. **9:00–10:00 — Recovery boundary.** Open **Checkpoints** and explain the paired files/context boundary and disabled unmatched snapshots. Do not restore or reset to bring back an earlier Runs view: applying the reply-only steer changes the current turn by design. Explain that a finished run or approved diff does not close the shared conversation; manual Complete/Reopen was removed in the September 9 simplification.

**Joseph's read-only Thread reply:**

> 请只读核对现有 ConversationScrollButton：默认名称是 Scroll to latest message，调用方传入 aria-label 仍能覆盖。只需简短说明是否符合，并把行为要求归属到 josephmreb1。不要改文件或运行检查。

**Spinsirr's read-only Thread reply:**

> 请只读核对已有回归是否分别覆盖默认名称与调用方覆盖，并把验证要求归属到 Spinsirr。不要重跑测试、修改文件、提交、推送或部署；不要执行这次 steer 之后新增的讨论。

**Optional later reply, after the two-reply thread is queued:**

> 后续讨论：以后再考虑 tooltip 本地化。这条不在刚才排队的两条要求中，不需要本轮实现。

**Spencer's check — one command against the existing change:**

> 请只读复核现有的无障碍名称改动。本轮只调用一次 bash，原样执行 pnpm test && pnpm typecheck && git diff --check，等命令完成后简短报告真实结果与退出码。不要改文件、安装依赖、提交、推送、部署、读取凭据或吞掉失败退出码。队友的待处理 Thread 由我们在本轮结束后的安全边界另行 Apply，不要提前处理。

**Hard stop at minute 9:** If the check or steer is still running or fails, preserve its real state and move to code at minute 10. Do not auto-apply after a failed check, retry repeatedly, clear the queue, or call old output a new pass. Describe a 429 as the observed failed attempt. The prepared result and earlier multiplayer acceptance remain dated evidence, not a substitute for today's result. Observe live text if present; a completed reply alone does not prove incremental delivery.

### 10–15 min — Code

Keep the walkthrough to three questions, with the actual functions open:

- **Who may start the next run?** `reduceTaskSession` / `canApplyNextSteer` in [`task-session.ts`](../src/lib/task-session.ts): discussion does not execute, `throughReplyId` freezes a whole-thread steer, and Apply waits for the current turn.
- **What if both people click?** `applyTaskSessionAction` in [`task-session-store.ts`](../src/lib/task-session-store.ts): the row lock covers the transition and grants execution once; a browser's timestamp is not a lock.
- **Whose instruction is this?** `buildHiveRunInput` in [`hive-prompt.ts`](../src/lib/hive-prompt.ts): frozen authored replies, promoter, and execution starter remain distinct. A shared avatar row alone cannot guarantee correct model attribution.

For Q&A, keep [`hive-runner.ts`](../src/lib/hive-runner.ts) ready for Codex/Sandbox execution, [`workspace-restore.ts`](../src/lib/workspace-restore.ts) for paired recovery, and [`use-shared-session.ts`](../src/hooks/use-shared-session.ts) for WebSocket recovery. These are backup references, not another live workflow in the five-minute code section.

### 15–18 min — AI journey

Use three concrete moments, about one minute each; the longer chronology below is for Q&A:

- **Human product direction:** AI leaned toward intent governance and long-lived rooms. Spencer brought it back to a real multiplayer agent conversation, one task per session, and a repository that can be attached later. The brainstorm records those corrections.
- **Human challenge, AI implementation:** Spencer caught that native session history could disappear with the sandbox. Postgres became canonical team history; saved sandbox/context pairs support resumable execution. Show the boundary, not a claim of disaster recovery after permanent deletion.
- **AI output needs evidence:** In the September 8 acceptance run, the coding agent issued malformed edit commands with `|| true`: shell exit zero did not mean the edit or tests succeeded. The supervising AI inspected the output, reproduced the misleading **Passed** label and shipped **Exit 0 / Exit N** with a real-component regression. Credit the diagnosis and implementation to AI; do not call it a manual human fix or a successful coding-task run.

### 18–20 min — Boundaries

Close on the shipped scope: one team, one task, one late-attached repository, one coding agent and explicit multiplayer control. No PR creation, organization administration, attachment ingestion or cross-task memory system is part of this demo.

Keep the evidence distinctions visible: original two-account execution and no-reload missed-message recovery passed; native incremental text passed separately at September 7, 00:19 UTC; whole-thread boundaries and paired restores passed later. On September 8, the nonempty queue survived restore and the same restored task subsequently completed its assisted coding/checks with correct authorship. These are separate observations, not a single uninterrupted happy-path run. Available snapshots are capped at three, and a snapshot without matching agent context cannot be restored. This is not permanent-sandbox-deletion disaster recovery. Do not call route navigation a completed timed rehearsal or submission.

## What is real today

- Dynamic task list and `/sessions/[sessionId]` routes.
- GitHub-authenticated humans with revocable, hashed database sessions.
- Seven-day signed invite links and explicit task-session membership.
- Durable transcript, Thread replies, frozen whole-thread steers, queue, review, and workspace state in Postgres. Presence and typing follow live connections.
- Row-locked mutations so concurrent teammate messages survive.
- Conversation with Hive before repository attachment.
- Team-level GitHub App installations and repository picker; one immutable repository per task.
- Fresh repository-scoped installation tokens for private Sandbox clones.
- AI SDK Harness with Codex and a persistent named Vercel Sandbox.
- Successful and failed Codex checkpoints persisted server-side; opaque resume state stripped from clients.
- Full on-demand read-only file navigation, command exit codes/output, and git diff; no fake PR, preview, test, or tool-result cards. Runs is the latest turn, not a run-history browser.
- IME-aware message submission that avoids duplicate CJK sends.
- Compact error states that preserve the human prompt and session.

## Current boundaries before final submission

- Complete the timed rehearsal on the final verified release. [Native incremental delivery](STREAMING_DIAGNOSIS.md), two-account execution/recovery, Thread boundaries, paired restore, nonempty-queue preservation and successful assisted continuation have separate dated production evidence below. An offline-mid-model-delta test remains unperformed; do not conflate it with the verified missed-human-message recovery.
- Retain and rehearse an inspectable real coding result; do not rely on artifacts that are no longer present.
- Branch push, PR creation, and organization administration are excluded by the current [goal](GOAL.md). Do not spend the remaining work on a separate delivery workflow.
- Match local credentials to the intended environment before using them as evidence. Previous temporary-database cleanup is a separate owner-controlled infrastructure action, not a reason to delete data during this goal.
- Obtain explicit owner approval for public repository access and submission, test reviewer invitations, and send all four items at least 24 hours before the scheduled presentation.

## Evidence log

### 2026-09-01

- Removed hardcoded preview, diff, command output, and fake PR artifacts.
- Connected real Postgres state and Vercel Sandbox execution.
- Verified simultaneous messages persist through serialized state changes.

### 2026-09-02

- Registered and installed the `Hive Multiplayer Agent` GitHub App.
- Separated GitHub OAuth identity from repository-scoped installation tokens.
- Verified a private repository clone in Vercel Sandbox.
- Transferred the Vercel project to the credited scope and verified Gateway OIDC.
- Reproduced duplicate CJK sends and added IME regression coverage.
- Added message annotations, explicit steering, and the shared attributed queue.
- Replaced verbose failures with compact system status.

### 2026-09-03

- Replaced the generic tool loop with AI SDK Harness’s Codex adapter.
- Persisted stable Codex IDs, resume checkpoints, and named Sandbox identity across successful and failed turns.
- Verified a resumed live Codex thread executed a real repository command and read `package.json` from the expected working directory.
- Removed browser-selected personas; all authorship now comes from a signed GitHub session.
- Reframed the domain from a long-lived room to a one-task session.
- Added the task dashboard, creation flow, lifecycle, signed invitations, explicit membership, and `/sessions/[sessionId]` routes.
- Added pre-repository planning turns and late, immutable repository attachment from the team pool.
- Removed the unfinished Vercel Connect path and kept direct GitHub App onboarding.
- Renamed database tables to distinguish `auth_sessions` from `task_sessions`, backfilled legacy members and GitHub installation access, and applied the migration without losing the existing demo session.
- Browser-tested task creation, pre-repo agent routing, message annotation, annotation-to-steer promotion, invite copying, completion, and reopening.
- Provisioned a durable Neon Free database through Vercel Marketplace, connected it under an isolated prefix, migrated all six Hive tables with matching row counts, switched production only after verification, and redeployed successfully.
- Production regression: GitHub sign-in, task listing, `Orbit Nav`, its attached private repository, and presence all survived the database cutover; public routes returned 200 and the unauthenticated session API remained 401.
- Verification: 27 focused tests, ESLint, TypeScript, and a production Webpack build pass.

### 2026-09-04

- Created a clean dogfood task against the real `spinsirr/hive` repository for the multiplayer demo.
- Removed the ambiguous workspace Overview tab; repository attachment is now a standalone pre-run state and connected tasks default directly to Diff, with Files and Runs as the only other artifact views.
- Replaced the read-only Terminal metaphor with Runs: a compact, team-facing audit trail where each command exposes its result, duration, and expandable raw output.
- Scope decision: the user added PR creation to complete the task handoff. Planned flow: review the diff, explicitly create a task branch and PR, and persist the real link for the team. This has not yet been implemented or verified.
- The real dogfood run returned Gateway 429 after several successful GPT-5 mini calls. The Gateway dashboard still showed $24.95 free credit, so insufficient balance is not established as the cause. Failed request: `gen_01M1PZHDC9HZCW0E9YCAN48JDA`, 2026-09-04 19:50:11 UTC. Resolving the underlying limit and completing a real code change remain open.
- Reproduced why that failure appeared as a generic error: the Codex bridge throws a raw string, but the error normalizer only read Error objects. Added a regression for raw, wrapped, and nested-cause errors, observed it fail, and fixed the normalizer. This repairs the message, not the upstream rate limit.
- Invite and Complete/Reopen now remain available as accessible icon buttons at narrow widths. Production commit `cc0f2c5` was Ready on Vercel; at the actual 737px browser viewport, Invite and Complete were both visible and enabled. The browser's attempted 375px override did not change the measured viewport, so do not claim mobile-width verification. Live clicks were blocked by the tool's safety approval because they generate an invitation and change task lifecycle; actual action verification needs confirmation.
- Added a lazy-loaded, read-only Monaco viewer with a monochrome syntax theme, file models selected by path, and same-origin editor/worker assets. Local browser verification covered TypeScript and JSON switching, CJK rendering, read-only DOM state, and same-origin script URLs. The temporary local fixture route was removed before deployment; this does not substitute for live agent artifact verification. Files still display bounded changed-file snapshots, not an editable or complete repository browser.
- Verification after these changes: all 27 tests, targeted ESLint, TypeScript, and the production Webpack build passed.
- Two-account testing is not complete. The second account stopped on GitHub's authorization endpoint with a 404. A likely cause is personally owned private App visibility (GitHub restricts sign-in to the owner); App settings are gated by the owner's Confirm access step, so the actual visibility and remedy still need verification. Keep repository visibility separate from App visibility.
- Read-only corroboration: the signed-in owner can view the App profile and existing installation, while an unauthenticated `GET /apps/hive-multiplayer-agent` returns 404. No App permissions or visibility were changed. The formal task transcript also survived the production reload after `cc0f2c5` deployed.
- Repeated a bounded real coding request in the same formal task at 22:45 UTC (add an author-specific accessible label to inline Steer Hive). It again ended with a 429, now displayed as “Rate limit reached. Try again shortly.” No successful code-change claim is supported. A separate no-repository control task was not created: the tool's safety approval requires explicit permission for that production state change.
- Reproduced a client synchronization race with controlled response ordering: delayed polling/action/broadcast snapshots removed a newer human message. Added one shared version guard to all three entry points and kept reset versions monotonic; the regression and cross-task-response checks now pass. This is deterministic local coverage, not a substitute for two-account live testing.
- Fixed the initial reload flash of an empty task by passing the existing server snapshot to the keyed task component. The page and session API now share one public projection, with a regression confirming that Codex resume checkpoints are excluded without mutating durable recovery state. Reconnection/focus also triggers an immediate refresh.
- Verification for the synchronization changes: 32 tests, targeted ESLint, TypeScript, and a production Webpack build pass. Full live network-loss and two-account recovery checks remain open.
- Production verification: Vercel marked commit `3af4c7a` Ready and assigned it to `hive-roan-mu.vercel.app`. Reloading the real task retained the attached repository, attributed conversation, latest coding request, and its failed-run status. GitHub App settings still require the owner's Confirm access; no second-user authorization or App-visibility change has been verified.
- Reproduced a safe-boundary bug: a failed run retained its queued steer but could not apply it, while the same action was incorrectly allowed before the preceding run finished. The task's `running` stage represented both active execution and pending work. A shared active-execution check now gates the reducer and UI; next-steer execution resets its own execution timestamps without replacing the durable Codex identity. New directions stay behind an existing queue after failure.
- Moved execution ownership into the row-locked transition. The session API now receives both the exact accepted state and whether that request started a run; it no longer infers ownership from equal wall-clock timestamps or a later snapshot. Same-millisecond inputs have deterministic regression coverage. This is not a claim that live two-user concurrency has been verified.
- Local browser verification used the real Hive component and reducer against a temporary, in-memory API fixture with no real accounts, database, or model calls. Verified: next steer disabled while running; enabled after failure and completion; refresh retains the pending queue; applying preserves the issuing member; removing the last pending steer restores review; Complete makes the composer read-only and Reopen restores editing. These checks did not perform the previously blocked production lifecycle/invite actions.
- A 375px iframe fixture reported its actual inner viewport as 375px. Rendered screenshots and the accessible tree showed Invite, Complete, and the queue action fitting the narrow layout. This supplements the earlier 737px production check, not a claim of a real-device production test. All five temporary fixture files were removed before the production build.
- Verification after the safe-boundary changes: 37 tests, targeted ESLint, and a production Webpack build including TypeScript pass. The production build route list contains no QA endpoints. Real coding remains blocked by Gateway 429, and second-account onboarding remains blocked at GitHub authorization.

### Onboarding security follow-up

- After the owner's Confirm access step, the GitHub App's Advanced settings explicitly showed **Make public**. The App is private; this confirms the suspected reason the second account cannot authorize. The owner approved making the App public only after adding invitation-only admission; the code repository stays private.
- Found why visibility alone was insufficient: any GitHub identity previously received a Hive account, could create its own task, and then query the single team's repository pool. The private App prevented unrelated accounts from reaching that path; no unauthorized access is claimed.
- Added admission inside account/session creation, before persistence: existing members retain access, the App owner is verified through GitHub's authenticated App endpoint for bootstrap, and new members need a correctly signed, unexpired invitation to an existing task. Merely installing the App is not admission. No new multi-team model or manual account allowlist was introduced.
- Invitations preserve their existing seven-day token format. Duplicate parameters, wrong-task or expired signatures, missing tasks, and external OAuth return paths are rejected. A denied account switch clears the browser's previous Hive login cookie and shows a compact invitation-required page. The invitation screen states that team members share authorized repository access.
- Verification: 49 tests, targeted ESLint, and a production Webpack/TypeScript build passed. Local rendering verified the invitation-required page. Tests use the real parser/signature/policy but simulate database and GitHub ownership lookups; this is not a two-account live sign-in claim. Local HTTP checks with dummy OAuth credentials also verified local return paths, an invalid-state callback returning 400, and an unauthenticated repository request returning 401; no real authorization was performed.
- Vercel marked `cc16780` Ready and Current on `hive-roan-mu.vercel.app` (deployment `AJiR1Mf4vKLT8FH9pbEQe3HpbTNP`). Unauthenticated production checks confirmed the new invitation-required page is live and both the task and repository APIs return 401. The previous deployment's unauthenticated URL redirects to Vercel SSO, protecting the old build.
- The Make public action was rejected by the tool's safety confirmation, which requires an explicit App-public authorization beyond the short affirmative reply. No retry or alternate mutation was made. The App remains private, as does the code repository; the second-account sign-in remains pending.

### Delivery reliability follow-up

- Reproduced draft loss in the actual Hive composer against a local failure fixture: a rejected POST cleared the unacknowledged Chinese message, and a later successful heartbeat hid the error. Added a per-submission delivery state rather than treating connection health as proof of delivery.
- Added failing state-machine tests for duplicate message retries, retries after an already failed run, and repeated annotation submission. Client-generated UUIDs are now validated at the API boundary and persisted with attributed contributions; the existing row-locked transition suppresses repeat execution for the same accepted submission. Identical text sent intentionally with a new ID, or by another member, remains independent.
- Drafts are stored per task, member, and annotation target in the current tab's session storage, with an in-memory path when storage is unavailable. Identity is persisted before the network request, and refresh restores an unconfirmed draft without sending it. A matching accepted contribution from either the response or normal synchronization clears it. A late response cannot clear a newer draft.
- Local browser verification used the real Hive component and reducer with controlled rejected, accepted, lost, and delayed responses; no model, database, or real GitHub account was used. Verified Chinese draft preservation, refresh without automatic resend, explicit retry producing one message, lost-response reconciliation, annotation refresh/retry, and delayed annotation acknowledgement. The latter exposed a stale callback closing a different message's composer; the corrected callback only closes its own target.
- Deduplication uses the canonical transcript and is scoped to the task and author (and target message for annotations). An explicit task reset clears that history; do not claim permanent exactly-once delivery across reset, or interpret accepted delivery as a successful agent run. Storage retention is limited to the current tab and browser availability.
- The full lint command initially scanned generated Monaco distribution files. Excluded only `public/monaco/**`, matching the existing generated-asset boundary; application source remains linted.
- Verification: all 64 tests, full ESLint, and the production Webpack build including TypeScript passed. All four temporary delivery fixture files were removed before the build, whose route list contains no QA endpoints. Real Gateway 429 resolution and two-account production recovery remain unverified.

### 2026-09-05 — Live multiplayer and mention input

- The owner made the GitHub App public. Its Advanced page now shows **Make private**; the `spinsirr/hive` repository still shows **Private**. The second real account, `josephmreb1`, successfully joined the formal task and its attributed messages appeared in the owner's browser. This resolves the previous authorization blocker.
- A production read-only smoke run returned the actual repository package name and test script. Runs showed the real command with exit code 0. This did not modify code or establish the full coding loop.
- During a subsequent real accessibility task, a new message from `josephmreb1` automatically entered the attributed queue. Refresh retained both users' messages and the queue. Apply next steer stayed disabled while the run was active and became enabled after failure. Annotation promotion, full two-browser recovery, and successful queued execution are still not verified.
- The accessibility run failed at 04:33:15 UTC after five successful model requests. Gateway request `gen_01M1QXF5YPDPQRDQ220X8R5GNG` returned 429; the corresponding single Hive invocation logged Codex's exhausted retry limit. Applying the retained `hi` steer also ended in a rate-limit error at 04:39. Do not claim the coding task or failure recovery succeeded.
- The dashboard showed $24.91 **Free Credit**. Three recorded longer runs each had five successes followed by a 429 within one minute. This suggests a free-tier per-model frequency limit, but the displayed error lacks the provider body or exact limit, so upstream limiting is not ruled out. [Vercel's rate-limit documentation](https://vercel.com/docs/ai-gateway/rate-limits) distinguishes credit balance from request rate and Gateway tiers from the site's subscription plan. No billing, model, or routing setting was changed.
- Added conversation `@` autocomplete using the existing Base UI dependency: only other actual session members are suggested, with name/handle filtering, caret-aware insertion, keyboard and pointer selection, and a compact monochrome popup. Selecting a leading GitHub handle uses the existing human-only message route; it does not add a new agent wake-up policy.
- Local interaction checks covered typing `@`, filtering, arrow-key and pointer selection, Enter selecting without sending, empty results, Escape preserving the draft, and a Chinese multiline message sent once. The test found and corrected the library's default Escape input-clearing behavior. IME submission guards remain covered by existing unit tests; this is not a claim of a hardware IME test.
- The temporary mention fixture uses no accounts, database, or model calls and was removed before the production build. Unit verification: 70 tests pass, including mention parsing, current-member exclusion, Chinese name matching, insertion in the middle of existing text, and compatibility with the actual teammate-message router. Full ESLint and the production Webpack build, including TypeScript, also passed.
- Commit `2a72474` deployed successfully. On the production alias in the formal task, typing `@` suggested the actual other member `josephmreb1`; typing `jo` filtered the list; Enter inserted `@josephmreb1 ` without sending; Escape dismissed the popup while retaining `@jo`. The test draft was cleared, no message was sent, and no model request was triggered. This verifies the deployed composer, not resolution of the Gateway limit or the full coding goal.

### 2026-09-05 — Honest review state and continuation check

- A bounded continuation of the same accessibility task was submitted at 05:06 UTC and failed with another rate-limit error at 05:07. The attributed request and earlier messages remained visible. This is evidence of retained product history, not successful code execution or native-history recovery.
- The earlier production read-only run had exposed a review-state bug: an empty diff could still be approved, producing a message about an unimplemented GitHub write layer. Three regression cases against the actual reducer reproduced it: successful read-only completion, approval of an invalid review, and draining a queue after a read-only result.
- Review eligibility now comes from the same predicate in the UI and reducer: an active task, attached repository, successful review state, nonempty diff, and no queued or applying steer. Empty results return to ready without discarding command evidence. Approval acknowledges the current diff only; it does not claim a PR exists or has been published.
- A temporary local fixture rendered the actual Hive component and used the real reducer with controlled responses, without accounts, a database, or model calls. Verified that both fresh and stale empty-diff states cannot be approved, a changed result can be approved once, and completing/reopening toggles read-only controls while retaining the transcript. The fixture was removed before the production build. This does not substitute for production lifecycle verification or the two-account test.
- A later read-only verification at 05:17 UTC recovered an actual changed-file diff. Its tests passed, but TypeScript failed because `LayoutProps` had not been generated. The model's summary incorrectly called the aria-label ternary incomplete; the captured HEAD diff showed both complete branches. Independent artifact inspection prevented an unnecessary rewrite. Do not use the generated summary as the source of truth for code.
- At 05:22 UTC, the existing workspace completed `node_modules/.bin/next typegen && node_modules/.bin/tsc --noEmit && node --test src/lib/*.test.ts && git diff --check && git diff -- src/components/hive/hive-workspace.tsx` with actual exit code 0. Expanded Runs output showed 27 passed, 0 failed on that sandbox's older repository revision. The production Diff showed both author-attributed Queue steer / Steer Hive branches; Files loaded its actual snapshot in read-only Monaco with file navigation and syntax styling. These 27 tests are distinct from the 73 tests in the current product checkout.
- Refresh retained the attributed transcript, successful result, and diff. This proves continuation of the existing task workspace and product history across earlier failures; it does not prove recovery after permanent sandbox deletion or a full network-disconnection scenario. Longer-run 429 behavior remains unresolved; no billing or model setting was changed.
- Added `pnpm typecheck` (`next typegen && tsc --noEmit`) and documented it, so a clean clone does not require a previous build merely to generate route helper types.
- Production completion/reopening was not performed: safety review required owner confirmation because completing the real shared task makes it read-only for collaborators. The task remains active. Only the local controlled lifecycle check above is complete.
- All 73 local tests, full ESLint, the generated-type check, and a production Webpack build passed. Commit `ee3c7b9` deployed to production successfully (Vercel deployment `EdQEcTHRR3Y85A8MWXG9wef5h2jd`, Ready). The production alias reloaded with the retained transcript and actual diff, rendered the updated check-mark approval control, and returned no browser warning/error entries in the inspected log. The local empty/changed-diff interaction checks were not repeated as production mutations; the shared task remains active pending owner confirmation for the lifecycle test.

### 2026-09-05 — Authorized production lifecycle check

- After the owner explicitly approved one Complete → refresh → Reopen cycle, performed it on the formal shared accessibility task at approximately 05:38 UTC, with no active run or queued steer.
- Completing the task disabled the composer, Send, and Reset; removed annotation controls; and showed the read-only explanation and Reopen action. Refresh retained this completed state, both authors' existing messages, and the real code diff.
- Reopening restored the composer, annotation controls, Complete, and the eligible diff's review action. The task is active again; the transcript and diff remain intact. No reset, diff approval, agent request, repository publication, or data deletion occurred.
- This is a production lifecycle check from the owner's authenticated account. It does not establish simultaneous visibility in the second account, network-disconnection recovery, or completion of the broader loading/empty/error-state checklist.

### 2026-09-05 — Shared incremental replies

- The user requested Streamdown and genuine WebSocket streaming, then explicitly approved replacing the 1.2-second polling loop and same-browser BroadcastChannel. The product still uses one authenticated mutation path; subscribing/reconnecting cannot start agent work.
- Reused the installed Harness streaming interface for Codex and AI SDK streaming for planning. Only public text events enter the chat; tool output and reasoning are filtered out. Separate public text blocks remain in the transcript rather than disappearing when the last-step summary arrives. Streamdown handles Markdown/CJK with monochrome code styling and a live caret; human contributions remain literal text.
- Reused Postgres instead of provisioning another service. The database remains the source of truth, with 250 ms coalesced response checkpoints, fixed reply IDs, sequence guards, and transactional notifications. Lightweight reply updates do not repeatedly send the workspace's files, diff, or Codex checkpoint. A direct LISTEN connection is shared by active viewers within each function instance and closed when its last viewer leaves.
- Added checks for incremental delivery before stream completion, filtering, abort/errors, batching/final flush, retained partial text after failure, out-of-order snapshots, late packets after completion, revoked authorization, socket cleanup, and slow-viewer backpressure, including coalescing while the database is slow. All 88 tests, TypeScript, full ESLint, the production Webpack build, and diff validation passed. The temporary UI fixture was removed before the build.
- Ran the opt-in transport diagnostic against the existing database: two independent LISTEN connections delivered to two real localhost WebSockets, reconnect loaded the latest fixture reply, and revocation closed both sockets with 4401. It sends transient notifications only; no task records or model calls were made. This is transport evidence with fixtures, not two-account production verification.
- Inspected the actual Hive Vercel project: Fluid compute is already enabled. The local Vercel CLI is not authorized; browser access remains valid. No billing, project settings, or credentials were changed. Production rollout and actual model-stream timing remain to be verified.
- Removed the remaining heartbeat-response snapshot path: presence POSTs now return 204 and cannot replace the transcript or claim the WebSocket is connected. Presence still triggers an authorized socket update. Repeated all 88 tests, generated-type checking, full ESLint, production Webpack build, and diff validation successfully after this change.
- Publication is pending explicit owner confirmation for pushing these changes to `spinsirr/hive`'s `main` branch and triggering production deployment. Automatic safety review rejected the combined commit/push request before execution; no commit, push, or production replacement occurred. Continue with the existing Git deployment workflow after confirmation, then verify the actual Vercel WebSocket runtime and model-stream cadence before claiming the feature is live.
- A subsequent read-only production check opened the formal task in the in-app browser and Chrome. Both authenticated as `Spinsirr`, retained the real accessibility diff and transcript, and showed the existing approved review state (the transcript records approval at 06:33). No new message, annotation, run, or lifecycle action was sent. The accessible Chrome profile is not the second member's login; this check cannot satisfy the two-account acceptance gate. The unshipped local changes remain at working-tree status above commit `13d4bd1`.
- The owner subsequently replied “上线” to the explicit `main` push and production-deployment confirmation. This authorizes releasing the tested realtime changes through the existing Git deployment workflow; it does not authorize making the repository public or sending submission materials. Deployment and live verification results are recorded separately below.

### 2026-09-05 — Realtime production release

- Pushed functional commit `a689b26` to `main`. Vercel deployment `Vivs17hWvWe5w3UJMeHQi6hQmpPh` became Ready and Current on `hive-roan-mu.vercel.app`; the dashboard reports a 50-second build.
- Opened the formal task on the production alias as the existing authenticated `Spinsirr` member. The new client reached Live through its WebSocket subscription, rendered 11 existing Hive replies through Streamdown, and retained the attributed transcript, actual accessibility diff, and approved review state. The inspected browser warning/error log was empty.
- At 16:15 UTC, independent unauthenticated requests returned 200 for `/`, 401 for the task API, and 401 for a real WebSocket handshake using the correct production Origin. No login cookies or other credentials were copied into this diagnostic.
- These release checks sent no human messages, annotations, agent requests, or lifecycle changes. They establish deployed rendering, authenticated live connection, retained state, and unauthenticated rejection—not actual model-delta cadence, network-loss recovery during generation, or two-account steering. Those acceptance gates remain open.

### 2026-09-05 — Login origins and review clarity

- The owner reported a GitHub callback error after opening a deployment-specific hostname. An isolated production build with fixture credentials reproduced the exact 400 response: the login route set a host-only nonce on one origin while GitHub returned to the configured callback origin. `scripts/check-oauth-origin.mjs` failed before the fix and passed afterward against real HTTP routes; it does not perform a GitHub code exchange or database writes.
- Login now redirects to the configured callback origin before generating any nonce, preserving task invitations and installation parameters. The browser's Host is checked because NextRequest normalizes loopback aliases in its URL; neither Host nor a return path controls the redirect destination. HMAC, nonce, expiry, invitation admission, and host-only cookie checks remain intact. Invalid callbacks clear the nonce and show a concise retry page instead of raw JSON. A fresh two-account authorization still needs live verification.
- The code viewer was tokenizing correctly but its custom theme had made tokens grayscale. Restored Monaco's standard light syntax colors while retaining white interface surfaces; disabled unused/deprecated-code fading for bounded read-only snapshots. Added green insertion and red deletion rows in Diff, retaining `+`/`-` markers and leaving file headers neutral. Color fills now span horizontally scrolled lines.
- A temporary page rendered the actual CodeViewer and DiffPane with isolated fixtures. DOM styles and a screenshot verified blue keywords, red strings, green comments, distinct green/red diff backgrounds, neutral `---`/`+++` headers, and read-only editor state. Removed the fixture before the production build. No repository content was altered by this visual check.
- Reframed the homepage around “Your team. One coding agent. Build together.”, shared tasks, and inviting, annotating, and steering together. No simulated teammates or new team-management features were added.
- All 92 tests, generated-type checking, full ESLint, the production Webpack build, and diff validation passed. Deployment of this follow-up and production visual verification are recorded separately; the wider multiplayer goal remains incomplete.

### 2026-09-05 — Production follow-up and real reply smoke check

- Commit `30b1727` reached Ready in Vercel (48-second build). Reloading the canonical production homepage in an authenticated Chrome browser confirmed the new team-focused headline, supporting copy, input placeholder, and Shared tasks heading. Deployment-specific URLs remain immutable; use `https://hive-roan-mu.vercel.app/` for the current release.
- The canonical login endpoint redirected to GitHub with the canonical callback origin and a host-only HttpOnly nonce. An invalid fixture callback redirected to the retry page without issuing a Hive login session. This was not a real second-account OAuth exchange.
- Before the next agent request, the formal task contained only the repository-connected greeting and had no current diff. This verification did not reset the task. The earlier accessibility change and approved diff are historical evidence, not artifacts currently visible in that task.
- At approximately 18:11 UTC, sent one text-only request through the production composer as `Spinsirr`, explicitly asking the agent not to use tools or modify files. A real Chinese response arrived and rendered through Streamdown. Refreshing the task and opening it in an independent Chrome browser retained the same complete response without duplication. Both browsers authenticated as `Spinsirr`; this is same-account cross-browser persistence, not the two-account acceptance test.
- The first browser sample arrived after generation completed, so it cannot establish incremental text cadence. A read-only checkpoint diagnostic found no matching task in the locally configured database; no database data was changed and that diagnostic is not production evidence. Its temporary script was removed.
- A follow-up text-only request was entered in the composer. The timing observer failed and the computer became locked before its send result could be verified. On resume, inspect the actual transcript/draft before retrying to avoid sending it twice. No incremental-cadence, in-flight reconnect, or two-account steering pass is claimed.

### 2026-09-05 — Submission readiness audit

- Reconciled the README, goal checklist, and demo route with the current user-provided goal: PR creation and multi-team administration are excluded. Kept the earlier scope discussion as historical context rather than a current implementation gate.
- Added `SUBMISSION.md` with links to all four required items, a two-paragraph draft, reviewer invitation requirements, owner-controlled publication/send approval, and the presentation-relative 24-hour deadline. Consolidated the blurb there to avoid divergent copies. No repository visibility or sharing setting was changed, and nothing was sent externally.
- Independent unauthenticated HTTP checks returned 200 for the canonical homepage, 401 for the formal task API, and 200 with a JavaScript content type for the same-origin Monaco runtime. GitHub repository metadata returned 404 through the current CLI credentials, so this did not verify repository visibility or reviewer access.
- Browser rehearsal remains paused because the computer is locked. The preceding unconfirmed send must be inspected before another agent request; a lock is not evidence that the server-side run stopped.

### 2026-09-05 — Resumed real coding and rate-limit evidence

- After the computer was unlocked, inspected the formal task before sending anything. The previously unconfirmed 18:19 UTC text-only request had completed exactly once, so it was not resent. Chrome's debugging connection was unavailable; the in-app browser remained authenticated as `Spinsirr`.
- At 19:02 UTC, submitted the actual accessible-label task through Hive, limited to the existing message-annotation button and the real `runActive`/queue condition. At 19:02:18 the browser displayed a 282-character public progress message while Complete remained disabled. Vercel logs show the run ended with a 429 at 19:02:28 and the HTTP request finished after 31.8 seconds. Refresh retained the attributed request, partial public reply, and compact error; this attempt did not yield passing checks or review artifacts.
- The Gateway log shows five successful `openai/gpt-5-mini` calls at 19:02:08, :13, :16, :19, and :22, then a 429 at :28. The rejected call reported no usage and completed in 0.24 seconds; its details did not expose a limit or provider error body. The dashboard still showed Free Credit. This supports a short-window limit but does not prove an exact requests-per-minute or token limit. [Vercel's current rate-limit documentation](https://vercel.com/docs/ai-gateway/rate-limits) separates the free-credit tier from purchased-credit limits and notes that upstream providers can also return 429. No model, billing, key, or permission setting was changed.
- After several minutes, sent one explicit continuation to use the saved checkpoint and check the existing diff before editing. A 205-character public progress message appeared. Reloaded while Complete was disabled; the same run was still visibly active after reload, with the transcript and partial reply present. The attempt subsequently reported another 429. Stopped model retries; no successful continuation or passing coding check is claimed.
- This establishes live public text before run completion and same-account refresh during an active execution. It does not establish token-by-token cadence, an offline network outage, two-account coordination, or successful recovery of execution artifacts. Those gates remain open.
- Re-ran the local 92-test suite and generated-type/TypeScript check successfully. All 18 relative document links and anchors in the README, goal, submission packet, and presentation notes resolved. These local checks do not substitute for the blocked real coding run.

### 2026-09-05 — Preserve evidence from failed runs

- A deterministic regression through the production coding runner and task-state transition reproduced a concrete recovery gap: a completed command and changed working copy were omitted from Runs / Files / Diff when the model subsequently returned 429. The Codex, GitHub, and Sandbox boundaries in this diagnostic are controlled doubles, not a real production execution.
- The runner now records tool events throughout the stream instead of depending on final result promises. On failure it captures the available working copy before stopping the caller-owned sandbox, with a bounded ten-second collection window. The failed task retains those artifacts and its available Codex checkpoint, stays in an error state, and cannot approve the diff. If collection itself fails, previous snapshots remain and completed command evidence is still saved; secondary collection errors do not replace the original run error.
- Unknown command exit codes now remain unknown and render as **Incomplete**, rather than being converted to exit code 0 / Passed. Git collection errors are not displayed as code diffs. Tools and reasoning remain outside the conversation.
- `scripts/check-failed-run.mjs` was red before the fix and now covers seven scenarios: a provider error, a thrown stream, a missing exit code, an unreachable sandbox, a Git collection error, a failed Codex checkpoint, and normal completion. It is included in `pnpm test` alongside the existing 92 tests. A temporary local page rendered the real Runs component and verified Passed / Incomplete / Failed plus expandable output; it was removed before building.
- This strengthens failure-state handling but does not resolve the Gateway limit or count as two-account, live execution-recovery, or network-outage verification. Deployment and live results must be recorded separately.
- All 92 existing tests plus the seven runner regressions, generated-type/TypeScript checking, ESLint, and diff validation passed. The default local Turbopack build was blocked first by font download access and then by its local port-binding requirement; the supported production Webpack build passed after permitting font access. No production build configuration or billing setting was changed.
- Functional commit `414b2b9` reached Ready / Production Current in deployment `B5ZNbF9PZgKbRLuZKVzKz1prHHVN` (33 seconds), with the canonical domain assigned. Independent unauthenticated checks returned 200 for the homepage, 401 for the formal task API, and 404 for the removed local fixture route. On reinspection the formal task was already Completed with its composer disabled; this turn did not complete or reopen it. Requested permission to reopen before sending any new agent request. No model calls or two-account pass are claimed for this verification.

### 2026-09-05 — Human presence is not agent status

- The header previously rendered every admitted teammate and an always-active Hive avatar, even when the task reported only one person online. It now derives avatars from the existing presence snapshot, omits offline people, and hides the stale strip when the client reports a connection error. Hive's execution state remains in the conversation and run UI; admission and mention suggestions still use the full member roster.
- Direct rendering of the actual avatar component verified one and two online people, departure, empty presence, unknown and duplicate IDs, accessible names, and no agent avatar. The temporary local browser fixture could not load because the existing development server stopped responding; it was removed without restarting that server. These controlled checks are not a real two-account presence test.
- All 92 tests plus the seven runner regressions, generated-type/TypeScript checking, ESLint, the production Webpack build, and diff validation passed before release.

### 2026-09-06 — Real continuation and inspectable checks

- After the owner said to continue, reopened the completed formal task without resetting it. Its existing transcript and repository remained. Production was running `aea1c97`; only the authenticated `Spinsirr` member appeared in the online-person strip. The second account was asked to join, but no simultaneous two-account verification was performed in this check.
- The 05:05 UTC coding attempt made the intended one-line change in `src/components/hive/hive-workspace.tsx`: an author-specific accessible label using `runActive || steeringQueue.length > 0` for Queue steer versus Steer Hive. It then returned 429. The deployed failed-run recovery retained five actual command records, including failed attempts, plus the real changed-file snapshot and diff. No passing checks were claimed for that failed attempt.
- Gateway showed five successful GPT-5 mini requests before the 05:06:00 UTC 429 and another new 429 at 05:10:13. Refreshing the Gateway log distinguished that second failure from replay of an old error. The account still showed $24.86 Free Credit; the exact request/token limit and retry window were not exposed. No model, credential, billing, or permission setting was changed.
- A 05:17 UTC continuation completed, but its claim of 92 passing tests was not backed by that turn's Runs. Only typecheck and `git diff --check --quiet || git diff --check || true` were recorded. Withheld acceptance: a zero exit code after `|| true` is not reliable proof of the underlying check.
- At 05:22 UTC, an explicit follow-up executed one real recorded command: `pnpm test && pnpm typecheck && git diff --check`. Runs contained the complete test summary (`tests 92`, `pass 92`, `fail 0`), successful Next.js route-type generation, and the combined exit code 0. This sandbox revision's test script does not contain the seven later runner-regression scenarios; those remain separately recorded local evidence.
- The retained diff still contained exactly the intended accessible-label insertion. Its row had a green background (`rgb(218, 251, 225)`) and `+` marker; the `+++` file header remained neutral. Files exposed the actual path in navigation and loaded read-only Monaco with blue keywords and red strings. This live change has no deleted line, so red deletion styling remains covered by the earlier controlled visual check, not by this artifact.
- Reloading the production task retained the attributed request and final reply, the passing combined command/output, and the available workspace. Public reply text was observed before the run became idle, but token cadence was not measured. This establishes successful same-account continuation after failure and refresh persistence, not network-loss recovery or two-account steering.
- Invite and Complete were visible and enabled at the measured production viewport of 810px. An attempted 375px browser override left the measured viewport at 810px and was reset; do not relabel this as a mobile-width test. The earlier 375px local interaction fixture remains the narrow-layout evidence. The available browser controls do not expose an isolated offline switch; no system-wide network setting was changed to manufacture an outage.
- No task reset, diff approval, sandbox commit/push, repository publication, or submission occurred. Preserve the current artifacts for the pending two-account rehearsal.
- Updated the README, goal, and submission packet to reference this inspectable result instead of the missing historical artifact. All 19 relative documentation links/anchors resolved, diff validation passed, and the local 92 tests plus seven runner regressions passed again. This follow-up changes documentation only; it does not turn the pending live collaboration checks into passes.

### 2026-09-06 — Two real collaborators and an attribution regression

- Verified the in-app task as `Spinsirr` and the native Chrome Joseph window as `josephmreb1`, with both people present in the header. The extension-controlled Chrome tab was actually another `Spinsirr` login; it was not used as evidence of Joseph's identity. Joseph's actions were performed in the authenticated native window.
- The owner's 06:12 UTC `@josephmreb1` message stayed human discussion. Joseph attached an annotation asking to preserve the author in the queued accessible label. Both windows displayed the actual annotation author, and no agent run started until explicit promotion.
- During the owner's 06:14 UTC read-only check, Joseph clicked **Queue steer**. Both windows showed Joseph's queued instruction while **Apply next steer** remained disabled. The first run completed with one recorded `pnpm test && pnpm typecheck && git diff --check` command: 92 tests passed for the sandbox revision, route-type generation and TypeScript succeeded, and the combined exit code was 0. Only then did Apply become available.
- The owner applied Joseph's steer. While that turn was active, Joseph sent a separate text-only follow-up. It appeared under Joseph's name in both windows and queued behind the active steer rather than interrupting it. Joseph reloaded during that turn and retained the shared transcript and partial reply. This was refresh during a run, not a manufactured network outage or a measurement of individual token timing.
- Acceptance failed on the agent's answer: it named `Spinsirr` as the annotation author even though the saved annotation and UI named `josephmreb1`. Stopped before applying the remaining message. No task reset, sandbox commit/push, diff approval, or repository publication occurred.
- The first diagnostic tested an incorrectly supplied caller identity. Inspecting the real route corrected that hypothesis: the route already used the queued steer's originating member. Its input instead lacked explicit annotation authorship and mixed the instruction with `Source: Spinsirr's message`. Extracted that production preparation into `buildHiveRunInput`; a regression through the real state transitions went red on the missing authorship fields. The fix separates annotation author, parent message author, steer promoter, and the teammate starting the run, using saved records and server-resolved members. Unpromoted discussion remains outside executable input.
- Found a related input loss at the pre-repository runner: it discarded the selected steer before calling the model. A separate regression calls that real runner with a doubled model boundary. Restoring the old discarded argument made it fail on the missing annotation; forwarding the selected input made it pass. These tests verify the input path, not that a model will always obey attribution correctly.
- Local validation passed: 98 unit tests, seven failed-run scenarios, the pre-repository input regression, ESLint, generated-type/TypeScript checks, and a production Webpack build. The original two-account case, the pending message's execution, offline recovery, and the timed rehearsal remain required. The Mac locked again before live retesting; no corrected production response is claimed. Release evidence is recorded separately.
- Pushed functional commit `df83d15` to `main`. The connected GitHub API reported a successful **Vercel** status for that exact SHA, linking deployment `6XCuSCjctg32JLF7JCdtunRKXF2P`. The same connector confirmed `spinsirr/hive` is still private. Independent unauthenticated requests returned 200 for the canonical homepage and 401 for the formal task API. These checks establish deployment success, availability, and login protection—not canonical-alias assignment of the new version or a corrected two-account response. Browser verification remains blocked by the locked Mac.

### 2026-09-06 — Production input retest and retained queue

- The Vercel dashboard confirmed deployment `G7yiRs1uxvxDdcjajoJwEx4RoDBH`, source `bd5a160`, as Ready / Production Current on `hive-roan-mu.vercel.app` (28-second build). Reloaded the owner task on that canonical alias before applying any queued work.
- At 06:44 UTC, the owner applied the previously retained Joseph message exactly once. The UI showed `Applying josephmreb1's steer`; the final reply correctly confirmed that the message came from Joseph and followed the earlier annotation review. The queue cleared and Complete became available. This is the original queued message's continuation, not a replacement message or a new annotation test.
- At 06:48 UTC, a text-only display check produced a real multi-paragraph Chinese reply. The first locator observation expired; inspection of the same run then found the public response and caret while Complete was still disabled. The next sample found the finished reply. Since the first successful text sample already contained all paragraphs, this still does not establish incremental character cadence. No run was restarted after the observation timeout.
- At 06:50 UTC, the owner annotated Joseph's existing message, with no names hard-coded into the annotation's requested answer. Saving the annotation did not start an agent run. The owner explicitly promoted it at 06:51; Hive correctly named Spinsirr as annotation author, Joseph as parent message author, and Spinsirr as promoter and execution starter. The result survived an owner refresh. This reverse, immediate-steer case is useful live evidence for the formatter, but it is not the original queued Joseph-author / owner-applier reproduction.
- The real accessible-label diff remained after these text-only turns. Files rendered the changed file in read-only Monaco with blue `import` tokens. Runs had no commands for the latest text-only turn; previous successful checks are dated observations, not currently visible command records. Replaced the misleading `No runs yet` empty-state copy with `No commands in this turn`, without changing state, layout, or command retention.
- The second account's native Chrome moved into a separate user workflow before the new annotation was entered. Stopped manipulating that window and requested a stable Hive tab for the original two-account retest. No new Joseph annotation was submitted, no offline network setting was changed, and no task reset, approval, repository commit/push, or publication was performed through Hive. The multiplayer gate and timed rehearsal remain incomplete.
- The copy-only UI follow-up passed 98 unit tests, seven failed-run scenarios, the real pre-repository runner input regression, ESLint, production Webpack compilation/TypeScript checking, and diff validation. It adds no dependency or state behavior; the existing white-and-black layout remains unchanged.

### 2026-09-06 — Original multiplayer retest, lifecycle, and offline-page recovery

- Used the canonical app with `356c525` deployed; its corrected command empty state was verified after reload. The GitHub connector reported Vercel success for deployment `3LUxyfNkYeKV7cFzq12HMnDHFkdn`. The existing native Chrome Hive tab remained stable as `josephmreb1`; the in-app tab was `Spinsirr`, and both human avatars appeared online without an agent avatar.
- At 07:04 UTC Joseph saved a new annotation with the exact wording of the original failing case, without hard-coding the expected author in the requested answer. Both accounts saw Joseph's annotation and no run began. The owner started one real combined read-only check; Joseph clicked **Queue steer** at 07:05 while it was running. Both accounts displayed the same attributed queue item and disabled Apply. The current command was not interrupted.
- The command `pnpm test && pnpm typecheck && git diff --check` passed. Before applying the annotation, the owner expanded the actual 92-test/typecheck output; Joseph opened the same command marked Passed. Native accessibility output truncates long command text, so this is not a byte-for-byte comparison of both command payloads. Both accounts inspected the real one-line accessible-label diff and the changed file; Joseph's read-only Monaco screenshot showed colored strings, keywords, and identifiers.
- The owner applied the queued annotation at 07:08 UTC. At 07:09 both accounts displayed the corrected answer: the annotation author was `josephmreb1`. Apply did not reattribute Joseph's instruction to the owner or the parent message. The queue emptied and Complete became enabled. Together with the original ordinary queued message's single execution at 06:44, this passes the bounded multiplayer sequence. The original failed result remains honestly recorded in the transcript.
- At 07:13 UTC both accounts showed the owner's completed task as read-only. Joseph refreshed, retained the disabled composer and Reopen control, then reopened at 07:14. The owner's controls restored through live synchronization; owner refresh retained the active task and correct author reply. The subsequent workspace check retained the original diff. No Reset or diff approval occurred.
- For a tab-scoped recovery check, saved an unsent Joseph test draft and selected Chrome DevTools Network → Offline. HTTP requests failed with `ERR_INTERNET_DISCONNECTED`, and the draft remained. However, the existing live connection still received the owner's text-only reply. Do not label that as interrupted-WebSocket recovery or claim it missed those deltas.
- Cleared only that unsent test draft, then reloaded the offline Hive tab. Chrome displayed its actual network-error page, closing the page's live connection. The owner started the final read-only check at 07:17 while Joseph was disconnected. Restored **No throttling** and reloaded via Chrome's toolbar; Joseph rejoined the same ongoing run with Complete disabled and the attributed request visible, without resending it. On completion, both accounts saw the same command and Passed status; the owner expanded `tests 92 / pass 92 / fail 0`, successful type generation/TypeScript, and the combined exit-code-0 result. The actual diff and prior annotation reply remained. DevTools was closed, the test network setting restored, and the latest command evidence left available.
- This proves recovery from an offline page reload while another teammate's run continues. It does not prove automatic live-socket reconnection without reload, measured per-character delivery, or disaster recovery after permanent sandbox deletion. No billing/model changes, sandbox commit/push, repository publication, or submission occurred. The timed rehearsal and owner-controlled submission gates remain open.

### 2026-09-06 — Automatic reconnect without reload

- At 07:24 UTC, put only Joseph's Hive tab offline again and kept the page open through the live connection's normal 270-second rollover. An unsent QA draft remained in the composer. This longer attempt deliberately addressed the limitation of the first short offline toggle; no application source, timer, server, or system-wide network setting was changed.
- The owner sent one leading-mention human message at 07:28. The owner's transcript contained one copy and its agent-reply count stayed at 16; Joseph's offline page had no copy. This established a real missed update rather than inferring disconnection from failed heartbeats alone.
- Restored No throttling without reloading the page. At 07:28:47 UTC Joseph displayed the previously missing message once and retained the draft. The Network panel showed a new `wss://.../live` request with status 101 and an open connection; filtering the panel later exposed the additional live-connection attempts from the test interval. This verifies automatic recovery of a human contribution without waking or resending agent work. It does not measure recovery of a partially delivered model reply.
- Cleared only the test draft without sending it, restored the empty Network filter and normal network setting, and closed DevTools. A final explanation-plus-check request started at 07:33. The real combined command passed with `tests 92 / pass 92 / fail 0`, successful route-type generation/TypeScript, and final exit code 0; its output was expanded at 07:35 and left inspectable in Runs. The stream observation missed intermediate character counts, so no typing-cadence pass is claimed and no additional model retries were made merely to obtain one.
- The documentation-only acceptance update passed all 98 local unit tests, seven failed-run scenarios, the real pre-repository input regression, ESLint, diff validation, and all 19 relative documentation links/anchors. Repository metadata/history filename checks found only the tracked `.env.example` and no committed `.pem`, `.key`, or local/production environment files; this limited check is not a complete secret audit or authorization to publish. The repository remains private and the materials remain unsent.

### 2026-09-06 — Continuous text observation and upstream boundary

- One continuous browser observation followed the existing 07:40:50 UTC request. Its first visible reply at 07:41:20.941 already contained 1,249 characters including the final check summary; Complete was still disabled. At 07:41:27.417 the finished rendering was 1,257 characters. The assertion requiring substantive incremental updates failed. No further model requests were made to retry this observation.
- Following `diagnosing-bugs`, a controlled differential test used the installed Codex event adapter, step tracker, real HarnessAgent, and Hive text writer. Genuine cumulative updates produce checkpoints before completion; completed-only events cannot satisfy that assertion. The test does not use a model, credentials, database, or live sandbox and is clearly kept under `scripts/diagnostics`.
- Official source for the pinned Codex 0.149.1 SDK/JSONL processor shows the missing event boundary: public message starts/deltas are not forwarded, while completed messages are. The currently published harness adapter 1.0.104 still uses this SDK path; it was inspected, not installed. OpenAI Docs identifies app-server's separate public-text delta events. See the [full diagnosis and sources](STREAMING_DIAGNOSIS.md).
- The existing run's actual combined command remained Passed, with 92 passing tests and successful type generation/TypeScript. The actual accessible-label diff remained. Corrected the current README/submission claims rather than presenting the controlled stream test as production proof. No production implementation, dependency, model, credential, billing setting, history, or workspace artifact changed. A native-event transport change versus explicit deferral is an owner scope decision; neither is silently assumed.
- Local validation passed: 98 unit tests, seven failed-run scenarios, the pre-repository input regression, ESLint, diff validation, and 23 relative documentation links/anchors. The differential diagnostic passed with incremental source events and intentionally failed the same assertion with completed-only source events. No production fix or successful native-delta retest is claimed.

### 2026-09-06 — Submission and route preflight

- Verified commit `0c9ffec` has a successful Vercel status for deployment `Gic5HmK1shE3hY5haysYtQQZHf5r`. The GitHub API still reports the repository as private. No repository-visibility change or external submission was made.
- Revisited the live homepage, its formal-task link, loaded read-only Files, retained real Diff, expanded successful Runs, and the corrected Joseph-authored answer. The owner-side read-only walk took 170 seconds including tool round-trips. The separate native Chrome window was confirmed as Joseph on the same task, with the corrected author response and latest transcript visible. No message, annotation, steer, model run, approval, or lifecycle mutation was made for this check; the owner still had 18 agent replies and an enabled Complete control.
- Tightened the presentation's live segment to use the already verified read-only annotation wording, one combined check, explicit per-step evidence, and an honest slow/failure path. Reduced the five-minute code walkthrough to execution gating, row-lock ownership, and authorship, keeping runtime details for Q&A. These preparation changes do not establish a completed narrated rehearsal.
- A read-only content-pattern scan covered all 55 commits reachable from local refs for common GitHub/OpenAI token formats, private-key markers, and password-bearing Postgres URLs. Its only candidate path was `.env.example`; all 108 URL passwords across its 54 historical versions were recognized placeholders. No matching credential value was printed. This is a bounded pattern check, not a complete secret audit, a scan of inaccessible remote refs, or permission to publish.
- Still needed: the owner's Codex streaming scope decision, summary review, presentation date/time, full narrated rehearsal, and explicit authorization for repository publication and sending the four items. Reviewer invitation/sign-in must be checked with the eventual reviewer access; an existing signed-in owner is not that test.

### 2026-09-07 00:19 UTC — Native streaming shipped and accepted

- The owner authorized the native transport change. We kept Vercel's existing harness lifecycle, authentication, authenticated bridge and saved thread IDs, and replaced only the sandbox turn driver with Codex app-server stdio. This is a useful scope decision: own the event mapping required by the product interaction while reusing the surrounding runtime. No dependency/model upgrade, billing change, PR feature, repository reset or new task was needed.
- Tests at the existing public stream/checkpoint boundary verify growing text before completion, no duplicate completed prefix, reasoning isolation, same-thread resume and partial-command evidence after failure. A second diagnostic uses the real pinned CLI with a loopback-only model fixture, proving on-disk history across two fresh processes and an actual command's output/exit code without model cost. An ESM-only dependency-resolution issue was caught there before deployment. Existing 98 unit tests, seven failed-run regressions, the conversation-input regression, TypeScript and ESLint pass. The supported webpack production build passes, including the sandbox assets; local Turbopack still encounters its port-binding permission issue.
- Functional commit `4a1abb3` deployed successfully as `FEK7aAcWYRPv6eVuGptwxcBD2m4X`. One bounded request in the existing canonical formal task produced **25 strictly growing updates** while Complete was disabled: **32 → 1,148 characters**, observed continuously between **00:19:28.481 and 00:19:36.081 UTC**. The first-update screenshot showed only the opening text. Exactly one new reply was added (18 → 19), and a fresh page restored the identical final text and returned to Live without starting work.
- The same run's expanded Runs entry was exactly `/bin/bash -lc 'pnpm test && pnpm typecheck && git diff --check'`, marked Passed. Output showed **92 tests / 92 pass / 0 fail** for the retained sandbox revision and successful type generation/TypeScript. The original `8cf1187..3624515` accessibility diff and Files snapshot remained. No new two-account or offline-mid-delta test is claimed; the earlier multiplayer evidence remains separate.
- The native-streaming gate is closed. Remaining human gates: summary personalization, full narrated rehearsal, presentation date/time, reviewer access, and explicit publication/submission approval. The repository remains private and no materials were sent.

### 2026-09-07 — Give conversation and code adjustable space

- User feedback: the fixed 420px conversation column wastes space when reading longer messages and cannot yield more space when reviewing code.
- Keep the same two-pane hierarchy and monochrome divider; make only the main conversation/workspace boundary resizable. Use shadcn-style primitives backed by `react-resizable-panels` 4.12.3, with a wider pointer hit target, keyboard resizing, a 360px conversation minimum, a 400px workspace minimum, and double-click reset. Width is a local view choice, not shared task state or an agent action.
- Keep both panes mounted across the existing 960px breakpoint. Narrow screens still switch views; resizing or switching views must not recreate the draft or Monaco editor. Preserve the editor's existing automatic layout behavior.
- Browser verification used the actual split component and Monaco in an isolated local fixture (no agent, authentication, or shared-session writes). At 1440px, dragging changed the conversation from 420px to 533px and Monaco from 811px to 698px. Home/End respected both minimums, ArrowLeft resized, and double-click restored 420px. At 390px and 959px only the selected pane occupied the viewport; at 960px both panes returned. The unsent Chinese draft and editor DOM identities survived view switching and breakpoint changes. No horizontal page overflow or browser errors. The temporary fixture was removed before release.
- Checks: 98 unit tests plus existing stream/failure/input checks, typecheck, lint, whitespace validation, and webpack production build passed. No backend behavior or saved task data changed.

### 2026-09-07 — Full workspace browsing and code-based collaboration

- User clarified that Files means **all files in the actual workspace**, not the runner’s first 12 changed-file snapshots, then asked for annotations the agent can see and sandbox checkpoints. Replaced the changed-file navigation with a lazy directory tree over the existing task’s Vercel Sandbox working copy. Reused the runner’s repository-directory rule; browsing never creates a sandbox, reclones the repository, or starts Codex. It can resume a stopped VM to read files.
- Keep the full tree out of the shared chat payload. Directories load 500 entries per page, files load on selection, and the current open folders/file refresh at a run boundary or on request. A late response for a previously selected file cannot display under a new filename. Text previews are bounded at 512 KB; hidden and untracked files are included, but Git internals, credential files, symlinks and unsupported binary/non-UTF8 files are not previewed.
- Monaco selection now becomes an attributed code reference with file path, line range and the reviewed quote. Sharing adds an ordinary discussion annotation to the existing conversation; it does not start a run. Explicit Steer Hive reuses the existing queue and prompt preparation. The original author is preserved even if another teammate promotes it. The agent is told to re-read the current file before changing it because a quoted selection may be stale.
- Kept draft acknowledgement, retry identity and IME-safe submission shared with existing composers. Did not add a second comment system, direct file saves, CRDT editing, or a new model/tool loop. Direct editing versus annotation-only was asked as a user choice; no answer was received before this bounded implementation.
- Checkpoints lists the actual sandbox’s saved, nonexpired snapshots using a metadata-only lookup. It neither resumes nor stops compute. The existing policy still retains one recovery point; retention and spending settings were not changed. No manual-save or restore button is claimed: Vercel snapshot creation stops its session, and safe rollback must also align the matching Codex history and team run state.
- Validation: 108 unit tests, existing native streaming/failure/input regressions, and real file/checkpoint/session route tests with doubled external services passed. The route tests verify membership, server-derived authorship, safe paths, actual filesystem content instead of saved snapshots, idempotent comments, and code context reaching the runner only after promotion. Typecheck, lint and the production Webpack build passed.
- Browser checks used the actual tree, Monaco, composer and split layout in an isolated local fixture: keyboard code selection, Chinese comment sharing, plain Enter staying in the draft, explicit submission, rapid slow-file → README switching, nested file opening, empty/binary states, all 507 paginated entries, explorer hiding and checkpoint rendering. At 960px and 390px, the explorer moved above the editor without page overflow. The fixture was removed before release. These do not replace a two-account production annotation test; the Mac was locked, so the authenticated live-workspace check could not be performed. No real task message, reset, approval, sandbox write, model call or rollback was performed for this feature’s tests.

### 2026-09-07 — Thread discussion, whole-thread steering, and paired rollback

- Replaced the Annotate entry and expanded inline comment cards with **Reply**, a compact reply summary, and a dedicated Thread pane. Human and completed agent messages both support discussion. Existing comments and their drafts retain their storage identity; this is one collaboration model, not two parallel systems.
- The owner asked to steer an entire thread. **Steer thread** submits the parent and all replies through the selected boundary, preserving each author. The server freezes the content at that boundary; a reply that arrives before the click reaches the server but after the selected last reply is not included. A duplicate click does not queue or execute the same boundary again. Newer discussion needs another explicit promotion. Conflicting requirements prompt clarification rather than treating the last speaker as the team's decision.
- Reuse the existing row-locked execution right, ordered steer queue, native agent runtime, and WebSocket snapshots. Thread replies never wake the agent themselves. No new task, queue service, inference model, or collaboration database is needed.
- Rollback restores **the sandbox files, private native Codex context, and recorded execution evidence together**. It requires confirmation, an idle active task, a current version, and a matching saved checkpoint. Team conversation and pending steers remain; approval is invalidated and nothing reruns automatically. This is not GitHub history rollback. Legacy snapshots without paired agent context are visibly ineligible.
- Normal recovery retention is now three snapshots. A temporary larger retention window prevents the safety save before rollback from evicting the selected target. The task remains durably paused if provider completion is uncertain; only the same operation can retry after its worker expiry. Once resume is confirmed, a retention-cleanup failure does not falsely report that restoring the work failed. New runs reapply the normal policy.
- Deterministic tests exercise the real HTTP handlers, state transitions and sandbox orchestration with external boundaries doubled: membership, server-derived authors, selected reply boundaries, one-time execution, private-data redaction, stale restore rejection, provider step ordering, and ambiguous-failure recovery. These are not evidence of a real production rollback.
- Local browser checks used the actual Hive workspace and composers over an isolated fake transport: existing replies, Chinese reply submitted once, keyboard mention selection without sending, whole-thread steer state, confirmation/cancel, attributed restore audit, 390px layout without horizontal overflow, draft retention across closing/reopening, and keyboard focus restoration. The temporary QA route was removed before release. No formal task messages, approval, model run, sandbox rollback or GitHub changes were made for these checks.
- Image/file upload remains a separate unfinished layer: private object storage and actual model ingestion must be connected before exposing it. The authenticated Vercel configuration check was blocked by the Mac's lock screen; no pretend upload button or public attachment storage was added.
- Final checks: 118 unit tests plus native-stream, failed-run and route regressions, TypeScript, ESLint, whitespace validation, and the production Webpack build passed. A failed-run checkpoint keeps its error state after restore, so restoring unfinished work cannot turn it into an approvable result. No two-account production thread/rollback check is claimed.

### 2026-09-07 — A small signed-in task dashboard

- The owner wanted the homepage to feel like a dashboard, then explicitly narrowed it to a simple version. Replaced the large marketing headline and split hero layout with a compact task list, Active / Completed filters, repository names, update times, and a New task dialog. No sidebar, charts, productivity scores, new search service, or additional team-management surface.
- Kept the existing membership-scoped query and authenticated creation action. Only display metadata crosses into the client; no new database query, schema, or API was added. Active means an open task, not an agent currently running. This page is a loaded list, not a claim of live team presence.
- Local UI verification uses an isolated fixture, never the formal task or a production account. The homepage change remains uncommitted and undeployed, together with the previous Thread/rollback work, pending explicit approval for the default-branch release.
- Verification: 120 unit tests, the existing runner/route regressions, TypeScript, ESLint and the production Webpack build pass. The local browser check covered the desktop list, Completed filtering, New task focus, Chinese IME Enter suppression, Escape returning focus, and the 390px empty list. The test browser closed before the remaining create/phone-dialog checks; no successful production creation is claimed. The temporary QA route and its generated type stub were removed.

### 2026-09-07 — Retain a safe dashboard UI demo

- The owner asked to keep a UI demo. Added `/demo` as a permanent design-review entry, using the same task dashboard and New task dialog as the signed-in app. Kept its scope to the dashboard instead of copying the workspace or mocking an entire agent transport.
- The preview clearly labels invented data. Its actions use page-local state only; task selection opens sample details, never a real task route. No authentication, database, GitHub, session API or agent runtime is imported into the demo. Refresh or Reset demo restores the samples. The route is statically prerendered and marked noindex.
- In the in-app browser, checked creating one Chinese-named task from the Completed filter: the modal closed, focus returned to New task, and Active showed the new row once. Task details and Escape focus restoration worked. At 390px, checked the empty state, new-task dialog, whitespace rejection and Chinese task creation with no horizontal overflow or recorded browser errors. Reload restored the original four samples. This is not a native input-method composition test or a connected task creation test.
- 122 unit tests, existing controlled runner/route regressions, TypeScript, ESLint, whitespace validation and the production Webpack build passed. The demo is open locally in the in-app browser; these changes remain uncommitted and undeployed pending the previously requested release approval.

### 2026-09-07 17:35 UTC — Reconcile presentation claims with release state

- Rechecked the canonical homepage without authentication: HTTP 200. The formal task API returned HTTP 401 without authentication. GitHub reports `spinsirr/hive` private, remote `main` at `1cd457ea507b47eac22c43896c43ddd8c72e20df`, and a successful Vercel status linking deployment `8GEeYBrq8jodhJ3tUVewQTaQpMos`. These checks prove availability, the login boundary, repository visibility and that commit's deployment status; they do not prove the current alias assignment or a fresh two-account session check. The read-only Vercel CLI inspection could not resolve the requested scope, so no stronger alias claim is made.
- Corrected the current 20-minute script: its preflight still pointed to the older 07:40 run, and its closing section still called native streaming unresolved despite the recorded September 7 acceptance. Preserved the original failed observations in the historical evidence log. Added a clear release boundary in the README and submission packet so the local Thread/rollback/dashboard work is not described as already available at the live URL.
- The narrated rehearsal, final-release acceptance, summary review, presentation time, reviewer access and owner-controlled publication/submission gates remain open. No production message, model execution, repository visibility change, push or deployment was performed in this audit.
- A subsequent fresh owner-page inspection completed at 17:43 UTC: the Annotate UI was still live, the real one-line accessible-label Diff remained, and expanded Runs retained 92 passing sandbox-revision tests plus type generation under the original successful combined command. The complete file tree loaded; opening the unchanged README proved navigation was not limited to changed-file snapshots. These are reads of the existing sandbox and retained run, not a new model execution, code modification, or production verification of the pending local Thread/rollback changes. Only the owner was online. Left the real Runs evidence expanded and kept the separate local dashboard demo available (HTTP 200).
- Whitespace validation and all 26 relative documentation links/anchors across seven Markdown files passed. No application source changed during this readiness audit.

### September 7 — Authorized dashboard, Thread and rollback release

- The owner explicitly requested a push and deployment. Commit `fec24843c3098efd7bafffb6220bbf211eee1839` was pushed normally to private `spinsirr/hive` on `main`. GitHub reported Vercel deployment `3VriTJeRNPk5naN15UWCj26JohXu` successful. No repository publication, submission, or account/billing changes occurred.
- Fresh release validation passed: 122 unit tests, all controlled native-stream/runner/route regressions, TypeScript, ESLint, whitespace checks and the production Webpack build. A bounded staged credential-pattern check passed; this is not a complete publication audit.
- Canonical-site verification completed around **18:04 UTC**. The signed-in homepage shows the actual two active tasks, Active/Completed filtering and an empty Completed state. New task opens and cancels without creating a production task. `/demo` returns HTTP 200; its sample task creation, empty state and reset work entirely in page-local state. Unauthenticated access to the real task API remains HTTP 401.
- On the real retained task, Reply opens a dedicated Thread containing both existing Joseph-authored replies and their prior steering labels. The whole-thread steer control is visible. No reply was sent or promoted. This confirms the UI and preserved authorship, not a new two-account execution.
- The actual accessibility Diff remained; expanded Runs retained the original successful combined command with 92 sandbox-revision tests, type generation and no failed tests. Full Files navigation loaded both the changed component and unchanged README in Monaco. File reads can resume the existing sandbox; they do not run an agent or write repository files.
- Checkpoints loaded an existing legacy recovery point with **No matching agent checkpoint**, and Restore was correctly disabled. No new checkpoint or restore was attempted. A real paired restore remains an explicit acceptance gap; do not infer a pass from the local provider doubles.
- Inspected production task and demo browser logs contained no warnings or errors. The retained execution evidence and team discussion were preserved. The separate public UI demo is the safe design-review link for remote viewing.

### September 7 — File-type icons in the workspace

- Replaced the generic file symbol with compact type-specific icons in both the Files tree and selected-file heading: TS/JS marks, React components, JSON, Markdown, styles, configuration, packages, scripts and assets. Directories retain open/closed states; restricted entries keep the lock and symbolic links keep the link symbol. Unknown types remain neutral.
- Reused the installed Lucide library and Next.js's built-in import optimization. No new icon package, CDN request, editor runtime, file read, permission rule or agent behavior was added. Icons are decorative so existing accessible filenames and navigation remain unchanged.
- Local verification passed: three filename-classification regressions (including configuration-name priority, uppercase extensions and unknown names), actual-component rendering checks for decorative/locked/folder states, the full 125-test suite and controlled runner/route regressions, TypeScript, ESLint, whitespace validation and the production Webpack build. These checks are not a new production agent execution.

### September 7 — Keep Thread replies visible in the conversation

- The owner pointed out that replacing inline annotations with only a reply count hid useful feedback. Thread now preserves the same reply records and explicit steering semantics while showing author and full body beneath the parent. The newest three replies are directly visible; older replies expand in place, and Open thread still opens the dedicated composer and whole-thread steering panel.
- Main conversation and Thread use the same reply component, including attribution and disabled/queue/steered states. Reading or opening the discussion does not promote it. Inline replies omit secondary timestamps; the opened Thread retains them. No database schema, queue, agent prompt or execution change was needed.
- Added a real-component rendering regression for visible text/authorship, safe text escaping, idle/busy/disabled steering controls, distinct promoter attribution, empty threads and chronological older-reply disclosure. The check uses invented display data and never writes to a task or runs a model.
- Follow-up visual review exposed a grouping error: a short right-aligned parent still had replies starting at the conversation's far left. Threaded messages now size and align as one parent-and-replies group, with a small inset reply rail. Messages without replies keep their existing layout. This is a layout-only correction; it does not alter discussion or steering semantics.

### September 7 — Cache Files without hiding workspace changes

- The owner asked to remove repeated loading when revisiting Files. Added SWR's in-memory request cache and cursor pagination, scoped to the task, member and current workspace version. Reopening files or folders reuses successful reads, and concurrent consumers share the pending request. No repository content is written to localStorage, IndexedDB, a shared server cache or a CDN; the authenticated file route remains private/no-store.
- Files retains its selected path and expanded folders across Diff/Files/Thread switches. The cache provider stays mounted while the hidden file browser and Monaco are fully unmounted. A never-opened Files pane does not prefetch the repository. The first actual visit to an uncached path still reads the sandbox.
- Run start/completion, sandbox or native-session replacement, restore and manual refresh discard the previous cache scope. Restore hides cached code and blocks reads until it finishes; late responses from a previous file or scope cannot populate the current editor. Unrelated chat and presence updates do not invalidate file contents. Cache lifetime ends on a new workspace version, refresh, task departure or page reload.
- Added actual Files-component, hook and SWR tests with a local DOM, React Strict Mode and a mocked read endpoint/Monaco surface: immediate cache hits, in-flight deduplication, directory paging and retry, view-state retention, hidden-pane behavior, version/member/task isolation, restore fencing, mismatched response rejection and stale-response races. These are UI/cache regressions, not a new live agent run or production rollback.
- Live browser verification caught an integration defect that a static editor double missed: React Activity cleaned up Monaco, then its React wrapper tried to reuse the disposed editor on show. Reproduced the minimal Files → Diff → Files failure twice, then added the actual Monaco React wrapper to the cache test (only its rendering engine is doubled). The test failed on disposed-editor access before the fix. Normal unmount/remount now resets editor refs while the separate data cache survives.

### September 7 — Wider inline threads and quiet conversation entry

- The owner clarified that the narrow element was the parent-message/inline-thread group, not the bottom composer. The first adjustment had enlarged the composer; commit `877061a` corrected the actual target by removing the thread group's shrink-to-content width. At a 390px viewport, the parent became 366px wide and the inset reply rail 354px, with no page overflow. This is an example of human clarification changing the implementation, not a correct first interpretation by AI.
- Opening a saved conversation visibly animated from the top because the existing `use-stick-to-bottom` component used `initial="smooth"`. Commit `396d80f` changes only initial entry to `instant`, retaining `resize="smooth"` for incoming text. No new scroll store, restoration effect or custom scrolling mechanism was added.
- A deterministic regression using the real Conversation component and installed scrolling library failed twice before the fix: successive sampled positions began `0 → 1217 → 2629 → 4095 → 5532 → 6896` instead of reaching the bottom on the first animation frame. Five checks now cover first-frame entry, hidden-pane re-entry, smooth following of new content, respecting a user's upward scroll/return action, and a newly mounted conversation.
- Release validation passed: 125 unit tests plus controlled runner/route/component regressions, lint, typecheck, whitespace validation and the production Webpack build. Production reload and Workspace → Conversation navigation both landed within one pixel of the latest message. No new message, model run or task-state mutation was performed for these UI checks.

### 2026-09-07 23:23 UTC — Submission preflight and remaining authority

- GitHub reported `396d80f` deployed successfully and `spinsirr/hive` still private. The canonical homepage and `/demo` returned HTTP 200; the unauthenticated formal-task API returned HTTP 401. These are availability/access checks, not reviewer sign-in acceptance or a new end-to-end run.
- Both inspected browsers were signed in as **Spinsirr**. Joseph's separate authenticated browser is still required for a fresh whole-thread multiplayer check. No impersonation or two-account result is claimed.
- A later text-only steer is present in the formal task. The current workspace is **Approved**, and Runs reads **No commands in this turn**. The earlier successful 92-test command was genuinely observed but is no longer the current command view. Fresh real command evidence is needed before rehearsal; do not present the historical chat summary as an inspectable run.
- Checkpoints includes a matching paired recovery point with Restore enabled and an older unpaired point with Restore disabled. Neither was restored. This improves the visible recovery prerequisites but does not prove a real paired rollback.
- A proposed new, isolated production acceptance task was denied by the safety review because it creates persistent application data; no task was created. Explicit owner authorization is required before that action. The approved formal task was not reset, restored or otherwise changed to work around the denial.
- Submission records were reconciled with these observations using the README skill's verifiable-claims principle. Current local/UI passes, historical production evidence and pending acceptance are deliberately separated. No PR automation, issue triage or cross-task memory feature was added. A narrated 20-minute rehearsal, owner-reviewed summary, reviewer admission, presentation time, repository-publication approval and authorization to send the packet remain open.

### September 7–8 — Authorized independent Thread and rollback acceptance

- After the earlier denial, the owner explicitly allowed a new, independent acceptance task, Thread testing and rollback, with the approved formal task untouched and test code not pushed. Created [Label the return-to-latest message action](https://hive-roan-mu.vercel.app/sessions/label-the-return-to-latest-messa-qvezdg) at **23:43 UTC on September 7**. Two actual GitHub identities, **Spinsirr** and **josephmreb1**, joined via invitation in separate authenticated Chrome browsers and both showed **2 online**. Attached `spinsirr/hive` after creation; the sandbox's real checkout was `54702e0`.
- The genuine small task was a default accessible name for `ConversationScrollButton`, preserving a caller's override, existing styling and scrolling behavior. Joseph authored the behavior requirement; Spinsirr requested two regression cases, locked dependency installation if needed, and `pnpm test && pnpm typecheck && git diff --check`, with no credentials, commits, pushes or deployment. The parent `@teammate` message and both Thread replies stayed human-only and were visible to both users.
- During a visibly active read-only baseline run, Joseph selected **Queue entire thread with 2 replies** once. Spinsirr saw one queued item attributed to Joseph; **Apply next steer** stayed disabled. Joseph then added a third reply about future tooltip localization. The existing queue still said **2 replies queued**, while the separate control offered three replies. The third reply was not promoted. This directly verifies the frozen boundary rather than assuming that the latest open thread is the run input.
- The baseline completed with a clean `git status --short`, actual base `54702e0` and no default accessible name. Its read-only command output was inspected in Runs. A paired **7:49 PM EDT** checkpoint was available. Only after this run ended did Spinsirr apply the queued two-reply steer. The queue emptied and both users retained Joseph's two-reply steering label.
- The implementation run failed with Gateway **429 at 23:51:42 UTC**. Real failed commands remained inspectable: `git apply` incorrectly received the other patch tool's format (exit **128**), and `python` was unavailable (exit **127**). The diff was empty. After six minutes, one explicit continuation was sent; Joseph reloaded while this continuation was visibly working and recovered the same visible request, incremental public text and discussion without resending anything. The earlier reload occurred after the first failure and is not evidence of a mid-run reload.
- The continuation also hit **429 around 23:59 UTC**. It left exactly one actual source addition, `aria-label={props['aria-label'] ?? 'Scroll to latest message'}`, and a successful `pnpm install --frozen-lockfile`. Runs retained five commands, including another failed patch attempt. Two edit commands used `|| true`, so their green command status is not proof of successful validation. No regression test was added and the requested test/typecheck command never ran. No successful model summary attributing both requirements is claimed. No further model requests were made after this second failure.
- Read-only Gateway inspection showed the first rejected request took **0.14 seconds**, with no recorded usage, while preceding calls had succeeded. Routing details, the provider error body and `Retry-After` were unavailable in that view, so the exact limiting tier/provider was not established. The dashboard said **No team budget**; that is not proof of unlimited request rate or a current paid-tier entitlement. The [current rate-limit documentation](https://vercel.com/docs/ai-gateway/rate-limits) distinguishes purchased Gateway credits from free-tier limits and warns that upstream providers can still return 429. The owner's gifted-credit classification was not confirmed; no billing, model or routing setting was changed, and a Vercel Pro upgrade was not presented as a proven fix.
- Before rollback, Files exposed the actual `node_modules` directory and the selected `src/components/ai-elements/conversation.tsx`; Diff contained the one-line addition. Confirmed Restore to the **7:49 PM EDT** baseline succeeded around **September 8, 00:06 UTC**. The real diff disappeared, the dependency directory disappeared from the reloaded root, and Runs returned to the single read-only baseline command. Joseph received the restore audit message and kept all three Thread replies. Files retained its selected path but reloaded both directory and content, verifying cache invalidation.
- A second confirmed Restore returned to the paired **7:59 PM EDT** partial-work checkpoint by **00:16 UTC**. The one-line diff and five recorded commands returned; the subsequently reloaded Files root again contained `node_modules` and the selected file opened in Monaco. The original rate-limit failure remained, with no approval action enabled and no automatic agent run. The Thread still showed **2 replies steered by josephmreb1**, with a separate option to steer all three. The task was left on this partial work, not marked complete. Stopping a running filesystem view also created unpaired safety snapshots; those stayed disabled in Checkpoints. Normal three-snapshot retention pruned older entries, so do not promise every checkpoint remains indefinitely available.
- Boundaries: these are real restore operations with checked filesystem and execution evidence, not simulated provider calls. Native model behavior after the restored Codex context was **not** tested because model requests had been stopped; the pending queue was empty during restoration, so preservation of a nonempty queue has deterministic coverage only. The original approved task was not reset, restored, messaged or rerun, and the sandbox code was not pushed to GitHub. Successful coding checks, post-restore continuation, the narrated rehearsal and owner-controlled submission gates remain open.

### September 8 — Retry the rejected request, not the task

- The owner asked whether repeated 429s were a code bug and whether a Codex subscription would avoid them. Read-only Gateway inspection showed **24.74 Free Credit**. [Vercel's rate-limit documentation](https://vercel.com/docs/ai-gateway/rate-limits) distinguishes free-credit limits from paid Gateway credits, but the failing request's routing details and error body were unavailable. A free-tier limit remains a supported hypothesis, not a proven exact provider/window. Codex's official ChatGPT authentication is a different usage pool, not an unlimited workaround; no subscription, credentials, model or billing setting was changed.
- The unchanged bridge was tested with the real pinned **Codex 0.149.1**, disposable native history, dummy credentials and a local Responses fixture. A 429 with `Retry-After` produced exactly **one HTTP request** before failing with “exceeded retry limit”; this was not evidence of a retry storm. The stronger regression ran one actual command, rejected the following model request with 429, and failed before the fix.
- Added a private loopback Responses transport inside the existing sandbox bridge, not a new service or task runner. It preserves harness credential brokering and native session management. Only rejected 429 requests may be repeated, with the identical body; the whole turn has two extra attempts and a 60-second waiting budget. `Retry-After` is honored without shortening an excessive delay; missing/invalid headers use jittered backoff. Native HTTP/stream retries are disabled on this Gateway route to prevent layered retries after ambiguous failures. Direct authentication is unchanged.
- Recovery is not a claim that the upstream limit is fixed. Persistent 429 retains its original status/body and stops; cancellation ends the wait, and an accepted stream is forwarded incrementally without buffering or replay. Structured diagnostics include only the configured model, status, bounded request identifiers, retry count and delay. The harness wrapper filters raw console logs rather than turning on the framework's general stderr debug sink. No error/progress chatter was added to the team conversation.
- Local verification: **132 unit tests**, existing runner/route/component regressions and the diagnostic-filter/bootstrap guard passed. Six loopback HTTP checks passed. The real pinned native process recovered one 429 after a command, emitted that command result only once, resumed the same on-disk thread, stopped after two additional 429 attempts, and made only one request on 502. The original red regression is now green. Typecheck, lint, whitespace validation and production Webpack build passed; the server trace includes the new transport asset. These are controlled local results, not a fresh production coding pass. Deployment and bounded live acceptance must be recorded separately.

### September 8, 02:36–02:38 UTC — Bounded recovery is deployed, not a coding pass

- GitHub confirmed `f3f602fa430a491a50b449227916992773805669` deployed successfully through Vercel deployment `3piq1xXPQAJ68RNXRkEwNJR2Uf7D`. One new normal message continued the existing independent task from its restored partial-work checkpoint. No new task was created, the approved formal task was untouched, and no sandbox code was committed or pushed.
- Gateway logs showed five `openai/gpt-5-mini` calls returning 200 between **02:36:58 and 02:37:17 UTC**, then 429 at **02:37:22**. The runtime's safe request diagnostics recorded two retries with delays of **17,455ms** and **33,764ms**, totaling **51,219ms**. The final 429 stopped at the attempt limit around **02:38:14 UTC**. No further model call was sent. No `Retry-After` or upstream request ID was present in those diagnostic fields; the exact rate window and whether Gateway or its provider imposed it remain unproven.
- Current Runs was inspected directly, not inferred from the transcript. It retained five commands: read/inspect operations and two malformed Perl edits. Expanded output showed syntax errors, but the command strings ended in `|| true`, so the shell reported zero. Diff still showed only the earlier default-label source line; no requested regression was added and the combined test/typecheck command never ran. Files loaded the real tree, including the dependency directory and selected source. Both actual members remained online and the three Thread replies remained attributed.
- This proves a native turn was attempted after rollback and bounded request retries occurred in production. It does not prove successful coding completion, correct post-restore interpretation of every requirement, or a resolved rate limit. Credits, model selection and authentication were unchanged. A proposed low-cost model comparison is awaiting the owner's choice.

### September 8 — Command evidence is not a test verdict

- The live failed edit exposed misleading product copy: Runs called any shell exit zero **Passed**, even when the command had masked an error. Kept the real exit code and raw output; changed the label to **Exit 0** / **Exit N**, with **Incomplete** for missing codes. The interface does not guess a test outcome by searching output for words such as “error.” No command, status, task state or model response is rewritten.
- Moved the existing Runs pane beside the other workspace panes to test the actual component. The regression first failed on a masked-error fixture displaying **Passed**, then passed with the corrected label. It also checks nonzero/incomplete states, verbatim escaped output, disclosure structure and empty turns. These are invented local fixtures, not replacement production evidence.
- Local validation passed: all **132 unit tests**, controlled native/runner/route/component regressions including the new three-case Runs check, TypeScript, lint without warnings, whitespace validation and the production Webpack build. All 27 relative links/anchors across the four submission documents resolved.
- Commit `4cf91a26660be8917b04b7ed95e7b1977ccbf963` was pushed through the already-authorized release flow; GitHub confirmed Vercel deployment `7TrjC23fZid7YJ34XVNvd6pKvNon` successful. On a fresh canonical owner page, all five retained commands displayed **Exit 0**, not **Passed**, and the original syntax-error output still expanded. Both members were shown online; this was an owner-side UI check, not a new two-account execution. No message, restore, model call, task approval, completion, or billing change was made. The coding task remains incomplete independently of this UI correction.

### September 8 — Reconcile the route with the current product

- GitHub reported the docs-only `c5b0505` deployment successful. The application behavior remains the `4cf91a2` Runs fix. Checked the canonical independent task in two existing browser profiles: one identity menu said **Spinsirr**, the other **josephmreb1**; both showed the same task and authored discussion. This was a read-only identity/state check, not a new two-user execution or fresh reviewer onboarding.
- Opened the actual Thread: three replies were visible, while its footer said **2 replies steered by josephmreb1**. The next action offered all three replies, so the revised route explicitly avoids re-steering that implementation thread. The current Diff still contained only the default accessible-label source addition. Runs retained the five commands, with the malformed edit's output expanded under **Exit 0**. Files loaded the repository tree, including unchanged files and dependencies, and the selected source in a read-only editor.
- Checkpoints loaded three entries: only one had a matching agent checkpoint and an enabled Restore action; the other two explicitly said **No matching agent checkpoint** and were disabled. No Restore was clicked. Available checkpoint IDs and retention change, so the route does not promise a particular old checkpoint remains restorable.
- Replaced the stale formal-task/single-annotation instructions with a timed target route for the independent task and whole-thread steering, exact two-author read-only review prompts, latest-turn Runs semantics and a minute-nine failure boundary. Updated the current product/domain wording and separated shipped restore behavior from unverified continuation. Earlier dated evidence is preserved below its original dates.
- Documentation validation resolved the relative links/anchors, checked five contiguous planned sections totaling 20 minutes, and passed `git diff --check`. These checks validate the script, not its actual delivery time. No application code changed; the application test suite was not rerun for this documentation-only revision.
- No message, steer, model request, restore, approval, completion, repository visibility or billing change occurred. The real coding check remains blocked on the pending model decision; this reconciliation does not complete the narrated rehearsal or any remaining acceptance gate.

### September 8, 03:48 UTC — Model compatibility is not account access

- The owner explicitly approved switching coding to `openai/gpt-5.6-luna` and continuing the same task, with no plan/authentication change. Only the coding default changed; planning remained `openai/gpt-5-mini`, and explicit low reasoning, native state, endpoint, credentials, prompts and retry limits were retained. The signed-in project's Project and Shared environment searches showed no `HIVE_CODEX_MODEL` override.
- The pinned **Codex 0.149.1** loopback test first failed because its fixture assumed the old JSON-schema tool array. Luna's native tools are embedded in the prompt and invoked through code mode. Updating the fixture to the actual custom-tool protocol verified a real command, first-turn history, the same resumed thread, the selected Luna model, low reasoning, and 429 recovery without repeating the command. No production bridge compatibility layer or SDK upgrade was added. **132 unit tests**, the controlled runner/route/component checks, typecheck, lint and production Webpack build passed; the model-override regression also passed. These fixtures use no account credentials or paid model.
- Commit `9662380` received a successful [Vercel deployment](https://vercel.com/spinsirrs-projects/hive/HzisaBgc1FVxWVry9JzK69CiKrVW). One continuation was sent in **Label the return-to-latest message action**. Gateway request `gen_01M1ZJ2GHYHS7PHK31BY7EYT7R` at **03:48:01.214 UTC** identified `openai/gpt-5.6-luna`, returned **403** in **0.53s**, and reported no usage and **$0.0000** inference cost. The runtime's original error explicitly said free-tier users cannot access this model and directed the owner to paid Gateway credits. This is a different failure from the earlier 429; it does not establish the earlier exact rate limit.
- [Vercel's current pricing documentation](https://vercel.com/docs/ai-gateway/pricing) separates Gateway free/paid credit tiers and restricts free credits to eligible models. A credit balance alone is not proof of paid-tier entitlement; do not promise that upgrading the site's Vercel plan activates these credits. No purchase, team transfer, provider override, new credential or automatic fallback occurred.
- **Spinsirr** and **josephmreb1** both displayed the same retained request, failure and three authored Thread replies. The original one-line accessible-label diff remained unchanged. Current Runs correctly showed **No commands in this turn**; no regression or check ran, and the task was neither approved nor completed. The approved formal task was untouched. Live coding acceptance and the narrated rehearsal remain open pending the owner's access/model decision.

### September 8, 04:07–04:15 UTC — Pro is not proof of Gateway entitlement

- After the owner said billing was complete, the signed-in team showed **Pro**. A single continuation of the same independent task produced Gateway request `gen_01M1ZK5919ZK4ZJA5F3TYQR8P3` at **04:07:00.393 UTC**: Luna, **403**, **0.29s**, no reported usage and **$0.0000** inference cost. The request retained the existing task, authorship requirements, excluded third reply and no-push boundary. It is not a successful coding continuation.
- The Gateway overview subsequently labeled its balance **Free Credit**. The error's official top-up redirect reached the correct team; opening the balance exposed **Buy AI Gateway Credit** and **Continue to Payment**, with auto-reload disabled. No amount was selected, payment continued, auto-reload changed or further model call sent. The owner must resolve paid-credit entitlement before retrying Luna; do not equate the Pro subscription or a positive free-credit balance with model access.
- This is read-only billing/access evidence and an actionable handoff, not a new application release, successful check or completed rehearsal. The fresh coding, post-restore native-context and narrated-demo gates remain open.

### September 8, 04:22 UTC — Keep the free-credit constraint explicit

- The owner chose to use existing free credits and then explicitly named **GPT-5.1-Codex Mini**. The official [model catalog with Free Tier selected](https://vercel.com/ai-gateway/models?freeTier=true&q=gpt-5.1-codex-mini) displayed the exact slug and **Free Tier: Yes**. This is eligibility evidence, not zero-price inference or unlimited capacity. No credits were purchased, auto-reload enabled or authentication changed.
- Changed only the production coding default, its example and directly related verification/documentation. Planning stays on GPT-5 mini, coding reasoning remains low, and the current task/worktree/native history are not reset. The SDK catalog call without local credentials returned 401; the public official catalog supplied the eligibility check instead. No credential was retrieved to work around that limitation.
- All **132 unit tests** and existing controlled runner/route/component checks passed, as did typecheck, lint and the production Webpack build. The existing pinned Codex loopback diagnostic now also accepts this exact model and verifies real shell output, same-thread history, the changed model, low reasoning and bounded 429 recovery. The original Luna/code-mode case remains available and passed separately. No production tool bridge or SDK version changed.
- Reading the production environment-variable page was rejected by the safety review because it could expose secrets. The verification will use the model name on the actual Gateway request instead; no indirect credential/configuration read was attempted. Deployment and the live coding task remain to be verified independently of these local passes.

### September 8, 04:26–04:33 UTC — Model access works; the coding check still fails

- Commit **`9953244cfc1ed749b183647af29c3db5d716d250`** received a successful [production deployment](https://vercel.com/spinsirrs-projects/hive/9gS3PRkKN2SsxVhFWrXoWRoHmvHp). One continuation of the same independent task was sent after refreshing the deployed app. Planning, credentials, low reasoning, history and bounded retry settings remained unchanged; the approved formal task was untouched.
- The actual Gateway model was **`openai/gpt-5.1-codex-mini`**, routed through Azure. Three requests returned **200** at 04:26:17, 04:26:38 and 04:26:41 UTC. The last request, [`gen_01M1ZM9AQT2K35Y5ZNSTVSH78X`](https://vercel.com/spinsirrs-projects/~/ai-gateway/logs?selectedLogId=gen_01M1ZM9AQT2K35Y5ZNSTVSH78X), reported 467 uncached input tokens, 38,144 cache-read tokens, 960 reasoning tokens, 495 other output tokens and $0.0042 total. Free Tier eligibility does not mean a zero-priced or unlimited model.
- Runs retained two source inspections and one exact **`pnpm test && pnpm typecheck && git diff --check`**, with **Exit 1** and the raw output expanded. The sandbox revision's **125 unit tests passed**, followed by several controlled checks, but `scripts/check-conversation-scroll.mjs:92` failed with **Identifier 'defaultBtn' has already been declared**. The subsequent typecheck and diff-check commands were therefore not reached. These 125 tests belong to the task's older checkout, not the 132-test deployed application suite.
- Diff showed two identical added accessibility-regression blocks and the earlier one-line source label. Files still loaded the real tree and selected source. Both actual GitHub identities retained the same request, compact failure and all three attributed Thread replies; no final agent response demonstrating both authors was produced. No approval, completion, restore, commit or push of sandbox code occurred.
- Runtime [request `mq8qr-1788841570708-22e1c5f0c6aa`](https://vercel.com/spinsirrs-projects/hive/logs?search=level%3Aerror&selectedLogId=mq8qr-1788841570708-22e1c5f0c6aa&panelState=opened) recorded a subsequent model HTTP **429** at 04:27:23 UTC, then delays of **16,249ms** and **30,023ms**, totaling **46,272ms**, before stopping at the two-retry limit at 04:28:10. Diagnostics identify the model and Gateway route but not the exact limiting provider or quota window. The regression parse error and the later model rate limit are distinct failures; neither is evidence of successful acceptance. No repeat model request or billing change followed this observation.

### September 8, 14:32–14:44 UTC — Repeated model limit; nonempty queue survives restore

- The owner confirmed that the presentation is not yet scheduled and asked to prioritize speed. One continuation of the existing independent task removed the duplicated regression declarations. The exact `pnpm test && pnpm typecheck && git diff --check` still exited **1** at `scripts/check-conversation-scroll.mjs:84:32`: the added assertion searches for the button while it is intentionally hidden at the bottom. Inspection also found the caller-override case mounted without its Conversation provider. These are test defects separate from the subsequent HTTP 429; neither typecheck nor diff-check was reached.
- Gateway showed five successful `openai/gpt-5.1-codex-mini` requests between **14:32:26 and 14:33:09 UTC**, each with **41.9K–45.3K input tokens**, followed by 429s at **14:33:30, 14:33:45 and 14:34:17**. The [last rejected request](https://vercel.com/spinsirrs-projects/~/ai-gateway/logs?selectedLogId=gen_01M20Q1VNWB4E32PNTBY5B1QKT) showed Azure, **0.14s**, no usage, zero cost and unavailable routing details. The overview still labeled the balance **24.69 Free Credit**. The [official rate-limit guidance](https://vercel.com/docs/ai-gateway/rate-limits) distinguishes remaining credits from throughput: free-tier per-model limits remain, and upstream limits can still apply on paid Gateway credits. The exact limiting layer and RPM/TPM window are not established by this evidence. The local transport has a turn-wide two-retry / 60-second waiting cap; no further model call or billing/model change followed the failed turn.
- While that run was active, actual **josephmreb1** saved a new read-only review reply and explicitly queued it. The owner's page showed its text and author with Apply disabled until the run ended. At **14:41 UTC**, the owner confirmed restoring the current **10:34 AM EDT** paired checkpoint `snap_eEToiKnth8keB0PHY3Sf6aOcWMBh`. Both accounts retained the same one-item queue, original three-reply implementation discussion, two-file Diff and four commands including **Exit 1**. The restoration message appeared; nothing ran automatically and completion remained disabled. The queue was left pending, not applied or removed. This closes the nonempty-queue preservation check, not successful coding or a full rehearsal.

### September 8, 14:52–15:10 UTC — Assisted repair, leaner prompts and a real passing result

- The supervising AI reproduced the hidden-button failure in an isolated source copy of sandbox base `54702e0`, using the real Conversation component and existing scroll regression. The correct test scrolls away from the bottom before querying the button and mounts caller props through the existing provider. The repaired test passed, failed when the source label was removed, then passed when restored. This local proof used installed local dependencies, not a claim of sandbox acceptance.
- After the earlier nonempty-queue restore was verified, the assistant removed only its diagnostic pending steer; Joseph's discussion reply stayed intact and was not executed. Hive then received the tested repair recipe. At 14:52–14:54 UTC it performed four inspections and produced the correct two-file diff, but five successful model requests were followed by 429 and two bounded retries before any check ran. That turn alone was not a pass.
- Prompt inspection separately reproduced two avoidable duplications: the current human request appeared in both recent context and the active input, and resumed turns replayed public agent replies already owned by native Codex history. `a13b3a1` removes those copies, retaining recent attributed human context, explicit steer payloads, restore guidance and native resume. Fresh coding and planning retain prior discussion. Three new tests were red before the fix and green afterward; all **135 local unit tests**, existing controlled regressions, typecheck, lint, whitespace validation and the production Webpack build passed. Vercel confirmed the [deployment](https://vercel.com/spinsirrs-projects/hive/7yUSgMp3GDPYPxKHDtsquDLcFYdr) successful. No model/auth/retry change, history deletion or forced compaction occurred.
- One check-only request was delivered after deployment. Current Runs records exactly **`/bin/bash -lc 'pnpm test && pnpm typecheck && git diff --check'`**, **Exit 0**. Expanded output shows **125/125 sandbox-revision unit tests**, the controlled regressions, all existing scrolling behavior, the default accessible-name query, the caller override case and successful route generation/TypeScript. No `|| true` or extra command hid a failure. Fixture messages such as `429 fixture` and `Sandbox unavailable (fixture)` are intentional failure-path tests, not new live provider failures.
- The actual diff is limited to `conversation.tsx` (default label, caller override retained) and `check-conversation-scroll.mjs` (real-provider/default/override checks, original scrolling assertions retained). No lockfile change, localization implementation, sandbox commit, push or deployment occurred. Both authenticated accounts received the same concise final answer assigning the behavior to **josephmreb1** and checks to **Spinsirr**, with the later localization discussion unimplemented. This is successful continuation in the same restored task, using native history and supplied human context; it does not isolate which memory source the model used.
- Gateway recorded only two requests for this final turn: **15:01:38 UTC** and **15:02:11 UTC**, both **200** on `openai/gpt-5.1-codex-mini`. The [final request](https://vercel.com/spinsirrs-projects/~/ai-gateway/logs?selectedLogId=gen_01M20RMY9XB9RKBYWFKGHHN755) used **2,426 noncached input tokens**, **53,632 cache-read tokens** and **40 output tokens**, costing **$0.0023**. The list's 56.1K input total includes cached history. Existing context is still long; this shorter continuation after a pause is not a controlled demonstration that deduplication eliminated rate limiting or reduced total input across turns.
- The assistant operated the two real signed-in browsers for acceptance. Joseph's browser inspected the same Diff/Runs and approved it; the owner's browser received that approval and completed the task. Joseph refreshed the read-only task and reopened it; the owner's controls recovered. Both accounts retained the final answer and reviewed workspace, and the current successful Runs command survived. The task is left open and approved; the unrelated formal task remains untouched. No further model request was sent. The technical acceptance gates close; the narrated rehearsal, owner-owned wording and publication/submission approvals remain open.

### September 8 — Submission preflight and presenter handoff

- Rechecked `ce2741b`: the worktree was clean and Vercel reported the documentation release successful. Anonymous, cookie-free requests returned **200** for the homepage and `/demo`, and **401** for the independent task API. In-browser `/demo` showed its sample-data/no-agent banner, working empty state and New task → Cancel path. No real task was created from that preview.
- Both real authenticated task browsers retained the successful combined command, approved workspace and correct identities. Opening the original Thread showed **2 replies steered by josephmreb1** alongside the later third reply and the separate **Steer entire thread with 3 replies** action. Checkpoints listed the 11:02 AM and 10:54 AM paired points as restorable, while the newer 11:15 AM sandbox-only point was disabled for lack of matching agent context. No message, steer or restore was sent during this read-only preflight.
- Screened every blob reachable from the local Git refs at `ce2741b`: **80 commits**, **568 unique blobs**, **10,215,106 bytes**, with no size exclusions. Rules checked complete private-key blocks, common provider-token shapes, credential-bearing database URLs, signed tokens and literal secret assignments; matching values were not printed. Fourteen URL matches were verified as `user`/`password`/documentation-host placeholders across seven `.env.example` versions. One literal-secret match was a labeled HMAC test fixture. No other matches or tracked credential/private-key filenames were found. This is custom pattern screening, not a comprehensive security/privacy audit; repository publication still needs approval.
- Prepared [DEMO_BRIEF](DEMO_BRIEF.md) as a compact speaking/navigation card and corrected the full route's Runs explanation: a later reply-only steer legitimately changes the latest-turn view. The original success remains dated evidence, not something to reconstruct with a reset. The code walkthrough was rechecked against the actual state-machine gate, frozen reply boundary, row lock and attributed prompt functions. This read-only preflight does not complete the narrated 20-minute rehearsal.

### September 8 — Owner confirms the project introduction

- Spencer confirmed the existing two-paragraph project introduction. The submission packet and current readiness summaries now record that confirmation; the approved text itself is unchanged. This does not claim that Spencer originally drafted the AI-assisted wording.
- No repository visibility change, external message, new agent run or completed narrated rehearsal is authorized or implied by this confirmation. The remaining rehearsal and publication/submission gates stay open.

### September 8 — Workflow tradeoff research, not an architecture migration

- Spencer asked to research the Workflow tradeoff and record it for presentation. Official Workflow/Harness documentation and the actual request, state-store and checkpoint paths were inspected; the [research note](WORKFLOW_TRADEOFF.md) records sources and limits.
- The comparison uncovered official Harness workflow helpers and the distinction between persistent team state and durable execution. The current request-bound run path's hard-termination risk is a code-reading inference, not a new production incident or a completed fault-injection test.
- Added a short explanation and Q&A pointer without claiming this was the original design rationale. No application code, model, queue behavior, production task or deployment changed; the approved submission introduction is untouched.

### September 8 — Scope freeze and final technical preflight

- Spencer explicitly deferred Workflow and further feature expansion. The [scope freeze](GOAL.md#submission-scope-freeze--september-8) is the canonical decision; the research remains available for Q&A. The README was shortened from 3,543 to approximately 1,330 words, keeping setup, access boundaries, architecture, evidence and AI ownership while linking the detailed chronology here. The owner-confirmed summary in `SUBMISSION.md` is unchanged.
- At **17:16 UTC**, the current local application passed **135/135 unit tests** and every controlled runner/route/native-stream/component regression in `pnpm test`. `pnpm typecheck`, `pnpm lint` and `git diff --check` also exited 0. These are local regression results, not another live coding run. Intentional fixture failures in test output are not new provider incidents.
- GitHub reported `spinsirr/hive` **PRIVATE**, remote main at `e6f0d2b`, and that commit's Vercel status **success**. Anonymous requests returned **200** for the homepage and `/demo`, and **401** for the independent task API. No publication or external submission occurred.
- The two real browser identities remained **Spinsirr** and **josephmreb1**. The original Thread still displayed **2 replies steered by josephmreb1** alongside all three authored replies; the later localization reply remained excluded. Expanded Runs retained the exact successful combined check with **125 sandbox-revision tests**, component regressions, generated route types and **Exit 0**. Diff still showed only the source-label and scrolling-regression files.
- At **375 × 812**, the owner successfully completed the task. Joseph received disabled steering controls, refreshed the read-only task, and reopened it; the owner recovered the active controls. Invite then showed **Invite link copied** and produced the expected task URL with a nonempty signed invite parameter, checked without printing or saving its value. An initial unconfirmed clipboard attempt was traced to the browser-control clipboard bridge; a fresh page with an initialized bridge passed. No application change was made for that tooling failure and no invitation was sent externally.
- Files reloaded the actual workspace tree and opened both the changed regression script and the unchanged `package.json` in read-only Monaco. Visual inspection confirmed file-type icons and syntax colors. The temporary narrow viewport was reset. No model request, new steer, file edit, restore, reset or sandbox push was performed; only the documented completion/reopening lifecycle changed.
- This preflight and document cleanup are **not** a narrated 20-minute rehearsal. That rehearsal, a fresh reviewer's invitation/sign-in and owner approval for public access/external submission remain open. Do not manufacture elapsed rehearsal time from this maintenance session.

### September 8 — Neon egress incident

- Production logs around **17:41–17:42 UTC** reported `Your project has exceeded the data transfer quota. Upgrade your plan to increase limits.` (SQLSTATE `53000`). Session POST and live GET returned 500 even at the authentication query. This is a database quota outage, not the earlier model 429. The earlier technical preflight does not establish availability after this incident.
- The confirmed amplification path was **5-second heartbeat → snapshot notification → full task read per connected viewer**. The application then removed private Codex resume history and paired checkpoints only after Postgres had already transferred them. WebSocket delivery had removed browser polling, but had not made database reads lightweight.
- The fix separates **presence**, **reply**, and **snapshot** notifications. Heartbeats read only presence/member metadata; public page, API and reconnect queries exclude private recovery fields inside SQL. Internal execution and rollback readers keep the complete native history. Coalescing cannot discard a pending reply when a presence event arrives. No schema migration, provider change, model request, paid upgrade or production data deletion is involved.
- Red-to-green integration evidence: `pnpm test:session-egress` uses real local Postgres, the production live route and two real WebSocket connections with invented database identities. With a fixed synthetic native-history fixture, one heartbeat changed from **2 full-task reads / 622,404 decoded result bytes** to **0 full-task reads / approximately 760 bytes**. One reconnect changed from approximately **311 KB** to approximately **1.1 KB**, with private native context still retrievable by the internal recovery reader. These are serialized query-result measurements, **not Neon wire-byte accounting or a measured percentage of production usage**.
- The test also verifies paired checkpoint context preservation, empty-workspace projection, and database membership revocation on a presence notification. The real client-hook regression checks online/typing updates without a task refresh, text-delta preservation, and missed-message recovery against a late HTTP snapshot. All **138 unit tests**, controlled regressions, typecheck, lint and the production Webpack build passed. A separate local diagnostic passed two independent Postgres listeners, real WebSockets, reconnect and authorization recheck. Deployment and restored production database access are recorded separately when verified.
- Exact production usage attribution and quota-reset timing were not measured: the Neon console required owner email verification, and the existing CLI identity could not access this project. A successful probe of the locally configured database is not production-health evidence. Fixing the query pattern does not replenish already-exhausted quota. [Neon network-transfer documentation](https://neon.com/docs/introduction/network-transfer).
- Release **`52756f6`** received a [successful Vercel deployment status](https://vercel.com/spinsirrs-projects/hive/3AySXCySSH8MmACoihESRmntVsWM). At **19:26 UTC**, a fresh production task page still showed a server error; new production logs still reported `53000` and exhausted data-transfer quota. `/demo` independently returned HTTP 200. Existing owner/Joseph task pages were not refreshed or reset, and no new model turn was sent. The CLI confirmed the repository remains **private**. The disposable local test databases were cleaned up and the dedicated test Postgres stopped. Actual production recovery, two-account re-verification and the timed rehearsal remain blocked on database availability; a successful deployment does not close those gates.

### September 8 — Connection-owned presence

- Spencer questioned why a separate HTTP/database heartbeat existed after WebSocket streaming was introduced, and explicitly approved optimizing it. The earlier egress patch reduced the payload but retained the polling-shaped presence design; this follow-up replaces that path rather than merely making each heartbeat smaller. It does not change agent execution, task scope, models, or billing.
- Presence now belongs to each authenticated socket. Typing is a small, validated WebSocket message whose author comes from the server's authenticated connection; browser messages cannot start agent work. Local and cross-instance aggregation deduplicate accounts with multiple tabs, and one tab leaving cannot mark the remaining tabs offline. HTTP task responses carry durable data/rosters and cannot overwrite live presence.
- Native WebSocket ping/pong runs every 20 seconds with no database access. Existing Postgres `NOTIFY` carries ephemeral presence announcements rather than notifying every viewer to query a table. One renewal per active instance/task every 30 seconds handles abrupt instance loss; unchanged renewals do not trigger viewer queries. A crashed instance expires after 90 seconds. This explicitly retains a small coordination cost instead of claiming that a distributed deployment can determine every connection's state from one instance's memory.
- Local real-Postgres/production-live-route fixture with two synthetic authenticated sockets: during the nominal **31-second idle wait**, it recorded **0 pooled table queries / 0 decoded pooled query-result bytes; 1 ephemeral renewal with a 185-byte JSON payload**. The final run actually elapsed **62,985 ms** of wall time (host/scheduling delay), so this is a functional regression, not a steady-state throughput benchmark; deterministic timer tests separately enforce the 30-second renewal interval. Notification SQL responses, listener fanout, protocol framing and TLS are not included in that payload figure, so it is not Neon billing telemetry. A typing change returned **537 decoded pooled-query-result bytes**, with no task/presence-table read.
- A separate diagnostic exercised two independent Postgres listeners and real sockets with delayed relay calls: online/typing agreement, leave delivery before closing the last listener, reconnect and revocation passed. Unit coverage additionally exercises multi-tab ownership, unchanged renewal, hard-instance expiry, invalid/spoofed typing frames and missing pong cleanup. The client hook regression confirms no HTTP heartbeat interval/POST, live presence surviving a late HTTP response, and no duplicate refresh merely from focusing a connected tab.
- Final verification: **147 unit tests**, the controlled runner/route/component regressions, explicit real-database login expiry, type checking, lint, and the production Webpack build passed. The synthetic login's initial one-minute lifetime proved too short for the host-delayed idle check; the fixture now allows ten minutes and separately expires the login to confirm that the actual authorization gate still closes its socket. Production authentication is unchanged.
- Release status: **local implementation and validation only; not deployed in this follow-up**. No production database changes, old-table deletion, account/model calls, paid upgrade, publication or submission occurred. Production availability and the goal's remaining acceptance gate are still tracked in [GOAL](GOAL.md#completion-checklist); this local optimization does not replenish the exhausted quota.

### September 8 — Task-aware skills, tools and repository memory (local only)

- After the scope freeze, Spencer explicitly requested that the agent understand its Sandbox/multiplayer environment and interact with teammates, then asked to connect Mem0. The narrow addition is four native MCP tools (`get_context`, `reply_to_thread`, `search_memory`, `remember_memory`) plus one short collaboration skill—not a new agent harness or workflow engine.
- **Taste decision: memory is selected intent, not an inferred team consensus.** The agent can select a saved human message/reply, not submit invented summaries or entire transcripts. Mem0 receives the short statement verbatim (`infer: false`) with author and source IDs. Explicit consent is a skill/tool-description rule; the server enforces the human source, length and obvious-secret guard, not a separate approval UI or complete DLP policy. Search is on demand, capped at three and scoped to the GitHub installation/repository. Memories are fallible context, not instructions overriding the current task.
- **Reuse without losing history:** the generic harness's changed skills/tools configuration can request a native thread restart. Hive instead uses the existing Codex MCP configuration and explicit native skill input. The real-process regression first caught a silent failure: a skill path alone was not discovered. Registering the bundled root with process-local `skills/extraRoots/set` made the original same-thread test pass; no repository files or native history were reset.
- **Authority stays with the product:** the Sandbox gets a five-minute signed capability for the current task, member and run. Every tool call rechecks the live run and membership; the Mem0 key stays on the server. Context SQL excludes code artifacts/private resume data. Thread replies have a fixed Hive identity, are retry-safe and cannot approve, start, steer or interrupt execution. If clarification blocks work, Hive asks in the Thread and ends its turn rather than polling for an answer.
- Local evidence: 158 unit tests and controlled regressions (including capability expiry); real MCP client/transport; real pinned Codex 0.149.1 preserving a seeded native history while using all four tools, then recalling across a new native task; real local Postgres and the signed production route rejecting foreign/stale/revoked capabilities and oversized bodies while preserving workspace, queue and checkpoints. The latest idle fixture elapsed 31,001 ms with zero pooled table queries and one 185-byte renewal payload. Skill validation, type checking, lint and a production Webpack build also passed, including file tracing for the bundled skill. No real provider/Mem0 request was used in these checks.
- **Release/acceptance:** local only, not committed/pushed/deployed in this change. Mem0 key configuration and actual cloud save/recall remain pending; Neon's quota outage still blocks production task verification. The owner-confirmed submission blurb is unchanged. Do not present this addition as shipped or as a reason the existing production page failed.

### September 9 — Sample-task affordances and release preflight

- Spencer tried opening a sample task in `/demo`. The rows looked like real task links but only opened a dashboard preview. Keep the database-independent UI demo, but name it **Sample tasks**, put **Preview** on every sample row, and provide **Open Hive** in both the demo controls and preview dialog. Do not invent a sample conversation or route sample IDs into real sessions. This is an affordance correction, not a database recovery mechanism.
- The real-component regression first failed on the indistinguishable heading. It now clicks the preview row, verifies its honest explanation and real-app link, closes the dialog and verifies restored keyboard focus. A second case confirms that the connected dashboard still links directly to real sessions. No backend calls are permitted by the fixture. The local production build's `/demo` was also inspected in the in-app browser.
- Full preflight: **158 unit tests** and all controlled regressions passed, including the new dashboard check; lint, explicit typecheck, whitespace validation and the Webpack production build passed. Previously validated connection-owned presence and the four task-scoped agent tools are included in the release candidate. Actual Mem0 save/recall remains pending; no key is configured locally and no live Mem0 request was sent.
- Availability at **03:54 UTC**: the authenticated [production log view](https://vercel.com/spinsirrs-projects/hive/logs?search=level%3Aerror) still showed `53000` and exhausted data-transfer quota during login lookup, including a 500 at 03:54:31. A new real-task browser page failed separately; preserved older tabs are not evidence of recovery. One `SELECT 1` against the existing local configuration succeeded, which does not establish the state of the deployed configuration. No database URL, plan, task record or checkpoint was changed. CLI log access was unavailable; the browser supplied the current failure evidence.
- **Release update:** `96d1542` was pushed through the existing GitHub integration. Vercel reports its [production deployment](https://vercel.com/spinsirrs-projects/hive/CcH2GHvNXtuKd4ggGH9hoWaddPxo) **Ready** (55-second build), and a fresh canonical `/demo` page shows the new preview labels and real-app link. This ships the preceding presence optimization and task tools; their earlier “local only” entries describe pre-release verification. A fresh post-deployment real-task page still failed with digest `3364982151`. Actual cloud Mem0 acceptance and restored production/rehearsal remain open. No real agent request, task mutation, repository visibility change or billing change occurred.

### September 9 — Quota recovery and two-account recheck

- The owner completed the upgrade. The correct Vercel-managed Neon integration displayed **Launch**, and a fresh independent-task page recovered its real transcript and two-file Diff, then became **Live**. This closes the observed database-availability blocker; no database replacement, migration, assistant-initiated purchase or new deployment was needed.
- At approximately **04:49–04:58 UTC**, the real **josephmreb1** browser entered the same task as **Spinsirr**. Both showed **Live / 2 online**, with the correct two identities. Joseph expanded the recorded combined `pnpm test && pnpm typecheck && git diff --check` command: **Exit 0**, **125 sandbox-revision unit tests**, controlled regressions and successful TypeScript output. This is inspection of saved output, not a new execution or a claim about the deployed application's 158-test suite.
- The task already contained two newer owner Thread replies and a 04:45 AM agent reply when this check began. Its current action is **Approve changes**, not **Approved**. Those existing changes were preserved; the assistant did not reapprove, complete, reset, restore or rerun the task to recreate the earlier presentation state.
- Joseph's initially empty composer received one explicitly unsent Chinese test draft. Spinsirr saw **josephmreb1 is typing…**. After Joseph reloaded, the entire rendered conversation matched its pre-reload text, the draft survived and both accounts returned to Live / 2 online. The test draft was then cleared without sending; the composer was empty and Send disabled. This verifies refresh/draft/presence recovery, not a new offline-mid-stream or hard-worker-termination test.
- One new human-only Thread reply, explicitly labeled as assistant-operated Joseph-account verification, reached Spinsirr's already-open page **once**, with **josephmreb1** attribution and without a refresh. The agent-message count was unchanged. No Steer, Apply, approval or model execution was requested. This is real two-account transport/attribution evidence, not independent human review.
- Mem0's local key was absent. The Vercel project-variable list contained no `MEM0_API_KEY`, and Shared reported **No shared variables linked**; this checks visible configuration, not arbitrary per-deployment overrides or live Mem0 service access. The assistant prepared an **unsaved Production / Secret** form with only the key name. Owner key entry, a deployment with that configuration and an actual authorized cloud save/recall remain pending. No key was revealed, and no Mem0 or paid model request was made.

### September 9 — Mem0 key release and production configuration gap

- The owner minted the Mem0 key and saved it in Hive as **`MEM0_API_KEY` / Production / Secret**. The assistant filled only the variable name; the key was not revealed, copied into local files or recorded in chat. Redeploying the existing `437b52e` source with current settings produced [deployment `7qEBCLjXZ84pW8DLZbzSzvxHYuw2`](https://vercel.com/spinsirrs-projects/hive/7qEBCLjXZ84pW8DLZbzSzvxHYuw2), **Ready** on the canonical domain.
- A separate [save task](https://hive-roan-mu.vercel.app/sessions/verify-repository-memory-save-q1wdex) and [recall task](https://hive-roan-mu.vercel.app/sessions/verify-repository-memory-recall-i2lau8) were created and attached to `spinsirr/hive` without touching the prepared coding task. The save task contains one short Thread reply explicitly labeled as assistant-entered test data, not a team rule. One main request authorized saving only that reply verbatim and prohibited code/shell/publishing work; it was not resubmitted. No model request was sent in the recall task.
- The save turn ended in **Rate limit reached** at approximately **05:17 UTC**. Gateway showed five successful `openai/gpt-5.1-codex-mini` requests from **05:15:40–05:16:14**, followed by 429 at **05:16:17, 05:16:33 and 05:17:07**. [The final request](https://vercel.com/spinsirrs-projects/~/ai-gateway/logs?selectedLogId=gen_01M229JCP05BZTTFXMNCK3BCZ6) displayed Azure, **0.19s**, no usage and zero inference cost, but no routing/error-body details. This identifies a model-Gateway rejection, not its exact limiting tier/window and not a Neon or Mem0 response.
- A separate authenticated Mem0 check in **spinsirr-default-org / default-project** showed **No requests found**. Vercel's project-variable search found the production `GITHUB_APP_CALLBACK_URL` but no `HIVE_INVITE_SECRET`. The production session route supplies the MCP connection only when both are present; saving the Mem0 provider key alone therefore does not connect the agent tools. No cloud memory save/recall pass is claimed.
- **Configuration tradeoff:** invitations currently permit the existing GitHub-client-secret fallback, while agent-tool capabilities require the dedicated `HIVE_INVITE_SECRET`. Introducing a fresh dedicated value also changes invitation signing, invalidating previously issued invite links. This side effect was disclosed and left for owner approval; no signing key, fallback, model, billing or authentication setting was changed to make the test pass. Existing membership and login records were not edited.
- This was live configuration/acceptance work, not a code change or a fresh local-test-suite run. The signing setup and one successful cross-task Mem0 check remain open; the prepared coding result, repository privacy and submission permissions are unchanged.

### September 9 — Open signup with task-scoped collaboration

- **Product decision:** Spencer explicitly approved first-time GitHub signup and confirmed three acceptance seams: first login, creating one's own task, and task/repository authorization. Requiring an invitation to try the product was unnecessary friction; removing it must not turn the existing global repository pool into public signup access. Invitations still matter for joining someone else's work. This supersedes the earlier invitation-only admission decision, without adding multi-team administration.
- **Red → green evidence:** the actual OAuth callback initially redirected the new account to `signin=invite-required`. After opening signup, the fixture exposed a second failure: a newcomer saw the owner's and an unrelated private repository in the same App installation. Both now pass with actual Postgres persistence and production route code. A separate cross-site attach attempt initially succeeded and now receives 403.
- **Boundary choice:** ask GitHub for the current user's repository intersection at both list and attach time; never infer access from an installation ID, a shared task, or a previous list response. Reuse a maintained JWE library for an encrypted, login-bound, HttpOnly credential instead of storing raw tokens in shared task state. Expiration requires an explicit reconnect. No invitation key rotation, database migration, data deletion or automatic privilege inheritance is involved; runtime global-installation/admission paths were removed while historical database records remain intact.
- **Local verification:** first login and an empty dashboard; creator spoofing/anonymous creation denied; same-installation repo isolation; forged/revoked repo attachment denied; task/API/files/checkpoint/restore/live access denied for non-members; only a valid task-scoped invitation admits another account; missing/tampered/expired/cross-account GitHub credentials fail closed; logout revokes one login. These checks use actual routes, task page and local Postgres with controlled GitHub HTTP, not real new GitHub signups. Full regression: 149 unit tests plus controlled runner/route/component checks, typecheck, lint and production Webpack build passed. These local results do not establish fresh-account production behavior.
- **Release and browser acceptance — September 9, 19:24–19:31 UTC:** `be1922f` was pushed, and [deployment `GRyeFnwPq9k7EKaJYXEGRB7T2DHt`](https://vercel.com/spinsirrs-projects/hive/GRyeFnwPq9k7EKaJYXEGRB7T2DHt) reached **Ready** on the canonical domain. The existing real **Spinsirr** account created the independent [Verify first-use repository access task](https://hive-roan-mu.vercel.app/sessions/verify-first-use-repository-acce-gbkkz6) without a repository. Its old login correctly offered **Reconnect GitHub**; actual GitHub OAuth returned to the same task, the picker offered the authorized `spinsirr/hive` repository, and selecting it persisted an attributed connection message. A full reload preserved the repository, conversation and **Live / 1 online** state. No model request, sandbox execution, new invitation or mutation of the prepared coding task occurred. This verifies a returning account's real reconnect/create/attach flow, **not** a brand-new real account or two-account isolation in production.
- **Anonymous production boundary:** without cookies, the homepage and task page offered GitHub sign-in; the task snapshot, Files, Checkpoints and repository-list APIs returned **401**. The live endpoint returned **401** with the canonical same-site Origin and **403** with a missing or foreign Origin. No WebSocket was opened. A genuinely new GitHub account's first signup and authenticated cross-account/repository isolation remain production acceptance gaps; their controlled real-Postgres regressions above passed. Repository publication and external submission remain unapproved.

### September 9 — Subtracting the inert workspace footer

- **Human-directed correction:** Spencer questioned **Send Hive a task** and asked to simplify the product. It looked like a second execution step despite being permanently disabled in that state. The conversation already owns task submission; the workspace should show the result and offer a real decision when needed.
- Removed the duplicate prompt, passive state buttons and idle footer, including its reserved height. **Approve changes** appears only when the existing approval policy allows it. Conversation status, queued steering, recovery and task lifecycle controls are unchanged; no replacement menu or new execution path was added.
- Local verification: all 149 unit tests and existing controlled regressions passed, including approval eligibility and queue behavior; lint and the production Webpack/TypeScript build passed. These are local checks, not a new live coding run or approval of the prepared task.

### September 9 — Execution completion is not conversation closure

- **Human-directed correction:** Spencer questioned why a teammate should click Complete when the agent reports its own result. We separated the end of an execution turn from closing the collaborative conversation. Removed Complete/Reopen, the associated write locks, and Active/Completed dashboard filters rather than introducing another completion workflow. Tasks remain focused on one outcome, with all accessible tasks ordered by their last update.
- **Boundaries retained:** successful nonempty diffs still require human approval. A completed run or approved diff allows further discussion and explicit steering; it does not automatically approve work or start another run. Membership, ordered input, active-run tool access and checkpoint-restore fencing remain enforced.
- **Data and verification:** existing task/history records and retired database fields are preserved without a migration or rewriting prior completion timestamps. Real local Postgres verified that a legacy-marked completed task remains listed, accepts authorized repository attachment and attributed discussion, and rejects retired API actions. Unit coverage verifies finish → approve → discuss → steer in the same task and native agent session; restored/unauthorized/stale-run boundaries remain covered. All 149 unit tests, controlled regressions, typecheck, lint and production Webpack build passed. This is local evidence, not a new production model run.

## Questions to prepare for

- How is Hive different from tagging Claude in a shared channel?
- How is it different from multiple people opening one v0 session?
- When does Hive wake, and who can redirect it?
- What happens when two teammates steer at once?
- Why use an existing coding harness?
- Why does Hive use Postgres state transitions rather than Workflow today, and what survives a hard worker termination? See the [retrospective comparison](#workflow-tradeoff).
- Why one task and one repository per session?
- What survives if a Sandbox disappears?
- What is real versus intentionally out of scope?
- Where did AI help, and where did human judgment change the plan?
