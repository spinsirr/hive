# Hive — Submission packet

Spencer confirmed the existing two-paragraph summary on September 8, 2026. Its wording is retained unchanged. The dated coding/recovery checks below passed, but this packet is **not ready to send**: the timed rehearsal and publication/reviewer access gates remain open. Current availability and the separate Mem0 acceptance results are tracked in the [readiness checklist](GOAL.md#completion-checklist). The [post-review verification](POST_REVIEW_VERIFICATION.md) passes the source-task citation retest, deployed timestamp follow-up, existing second-account task creation and bidirectional task-page/dashboard isolation; first-ever signup and authenticated production API/repository checks remain open. The later [subagent checks](SUBAGENTS.md#september-10-pacific-live-owner-stop-acceptance) confirm saved completed results, an owner-operated live Stop without interrupting the parent, and refresh persistence; cross-account Stop and independently observed child tool execution remain unverified. This confirmation does not authorize repository publication or external sending. Nothing has been submitted.

The [September 8 scope freeze](GOAL.md#submission-scope-freeze--september-8) defers Workflow and additional features. Recovery acceptance refers to the dated browser-reconnection, caught-failure continuation and paired-restore checks below, not automatic recovery after hard worker termination.

Recorded coding acceptance: `a13b3a1` deployed successfully, removing duplicate prompt context while retaining native Codex history and the owner's selected `openai/gpt-5.1-codex-mini`. On September 8 at 15:01–15:02 UTC, the independent task completed one real `pnpm test && pnpm typecheck && git diff --check` with Exit 0: 125 sandbox-revision unit tests, controlled regressions and TypeScript passed. Both real accounts inspected the two-file result and the attributed final answer, then verified shared approval and Complete → refresh → Reopen. Earlier Thread/queue and paired-restore checks include preservation of a nonempty queue. The supervising AI supplied a tested regression repair after earlier failures; this was an assisted completion, not an autonomous first-pass result. Recurring 429 risk remains despite the successful continuation. The public [UI demo](https://hive-roan-mu.vercel.app/demo) is still a dashboard preview, not execution evidence. Deployment authorization does not authorize billing changes, repository publication or external submission.

The Complete/Reopen checks above are historical: Spencer [removed manual task completion on September 9](PRESENTATION_NOTES.md#september-9--execution-completion-is-not-conversation-closure). Current tasks remain open for discussion and steering after a run or approval.

## Four submission items

| Item | Location | Before sending |
| --- | --- | --- |
| GitHub repository and commit history | [spinsirr/hive](https://github.com/spinsirr/hive) | Keep private until the owner explicitly approves publication. Then verify access while signed out. |
| Live application | [hive-roan-mu.vercel.app](https://hive-roan-mu.vercel.app/) | Verify fresh GitHub sign-in and creation of an independent task. Also supply an invitation for the prepared shared task; homepage/demo HTTP 200 is insufficient. |
| Written overview | [README](../README.md) | Reconcile the verification section with the final deployed release. |
| One-to-two-paragraph summary | Below | Existing wording confirmed by Spencer on September 8, 2026. External sending still requires separate approval. |

## Summary blurb

Hive is a multiplayer coding agent for small software teams. Instead of one person privately prompting an agent and handing the result to teammates later, everyone shares the same task-scoped conversation and execution workspace. Teammates can discuss a specific message in a thread and explicitly steer Hive with one reply or the discussion so far, preserving who said what. The point is to keep the team's intent present while the code is being made, not just when it is reviewed afterward.

I built Hive as a focused Next.js full-stack product. GitHub sign-in establishes authorship, invitations admit teammates, and Postgres preserves the shared conversation and serializes their input. An existing Codex harness runs inside Vercel Sandbox; teammates inspect actual commands, changed files, and diffs. I concentrated on the multiplayer interaction—discussion versus direction, explicit queue boundaries, and shared review—rather than building another agent runtime or adding PR automation and organization management.

## Readiness gates

- The two-real-account annotation → steer → queue → execution sequence passed on September 6 at 07:09 UTC, including the corrected attribution case. Cross-account completion/reopening also passed; retain these results for rehearsal.
- Native text streaming is now [deployed and verified](STREAMING_DIAGNOSIS.md): the 2026-09-07 00:19 UTC run showed 25 growing updates before completion and one final reply, restored identically by a fresh page. Failed-run continuation, offline-page reload recovery, and no-reload recovery of a missed teammate message passed separately. The latter is not an offline-mid-model-delta test; do not describe fixture tests as production evidence.
- Preserve the fresh, inspectable result in the independent [acceptance task](https://hive-roan-mu.vercel.app/sessions/label-the-return-to-latest-messa-qvezdg): its 15:01–15:02 UTC combined check and actual passing output remain in Runs, alongside the two-file diff and attributed final response. Recheck these before rehearsal. Do not send a text-only model turn that replaces the latest command evidence, or reset/restore the unrelated approved formal task to manufacture a pass.
- The independent task now verifies human-only replies, frozen two-reply queuing, safe-boundary application, reload during a continuation, paired filesystem/context restores, nonempty-queue preservation and successful assisted completion after restore. The assistant operated the two real authenticated accounts for acceptance; do not call this two independent human reviews. No sandbox code was committed or pushed.
- Rehearse with the compact [presenter card](DEMO_BRIEF.md) and [exact interaction prompts](PRESENTATION_NOTES.md#demo-sequence-20-minutes), using a timer for problem → solution → code → AI journey.
- The read-only technical route has been checked in production, including the original frozen Thread boundary, current successful Runs and the public demo's empty/create/cancel paths. Anonymous homepage/demo requests returned 200 and the task API returned 401. The route targets the independent task, includes exact two-author review/check prompts and a minute-nine failure boundary, and leaves the historical task untouched. This is not proof of a 20-minute narrated rehearsal. Pattern screening at `ce2741b` covered all 568 blobs across 80 local reachable commits; matches were verified placeholders/test fixtures, but this is not a complete security/privacy audit or publication approval.
- The owner confirmed on September 8 that the presentation is not yet scheduled. Confirm its date and time when set, then send all four items at least 24 hours beforehand; do not invent a calendar deadline.
- Obtain the owner's explicit approval before changing repository visibility or sending materials. No invite token, credential, or private participant information belongs in the public README.

The presentation is 90 minutes in total, with about 20 minutes of demo and the remainder for discussion. The [evidence log](PRESENTATION_NOTES.md#evidence-log) and [goal checklist](GOAL.md) remain the detailed readiness record.
